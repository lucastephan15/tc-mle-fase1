# Backlog de revisita

> **Para que serve:** o TC é construído **em paralelo** às aulas dos Módulos 02 a 05. Quando uma
> aula futura trouxer algo que afeta uma etapa **já fechada**, a regra é: **não refazer na hora.**
> Anota uma linha aqui e segue. No fim, varre-se o backlog de uma vez, com o conteúdo fresco e
> sem perder o fio do que já funcionava.
>
> Isso existe para resolver um risco específico e nomeado: *"ficar refazendo tudo do zero e me
> perder no que estou acrescentando/editando"*. As outras duas metades da solução são o **git**
> (o que mudou) e o **decision log** (por que mudou).

**Como usar:** uma linha por item, com a etapa afetada. Marcar `[x]` quando revisitado —
e se a revisita mudar uma decisão, a mudança vira linha no `decision-log.md`, não some aqui.

---

## Pendências abertas

| # | Aula / origem | O que revisitar | Etapa afetada | Status |
|---|---|---|---|---|
| 1 | M01-A08 | ~~Confirmar previsões feitas antes de abrir o dataset~~ → **4/4 acertadas** (7.043 linhas · churn 26,54% · `Total Charges` texto com 11 vazios · `CustomerID` descartável). Errei só o nº de colunas: 33, não 21 — é a variante estendida da IBM | 1 | ✅ |
| 2 | M02 (a estudar) | Algoritmos e técnicas de validação novas → candidatos para a Etapa 6 | 6, 7 | ⬜ |
| 3 | M02 (a estudar) | ~~Conteúdo de redes neurais → arquitetura e regularização do MLP~~ → **a M02-A05 não entregou a técnica** (sem ReLU, Adam, dropout, batch size; hands-on é XOR com 4 pontos) e a **Etapa 8 foi executada com o runbook**: 1 camada oculta · `BCEWithLogitsLoss` · `Adam(1e-3)` · one-hot + padronização · 5 seeds · early stopping por PR-AUC. Funcionou: 42s de execução, 12 testes novos | 8 | ✅ |
| 4 | M03-A06 (13/08/2026) | **Refatoração de `src/` — o item deixou de ser "a estudar": a aula chegou** (era prevista para a M03-8; é a A06). Escopo agora **medido**, não suposto: **5 fábricas de estimador** (`comparacao.catalogo` · `train.construir_modelo` · `ablacao.montar` · `tuning.estimador_base` · `selecao.modelo`), **4 assinaturas de retorno diferentes**, **19 instanciações** em 6 módulos, e o campeão `LogisticRegression(max_iter=1000, random_state=SEED)` escrito **6× em `src/` e 6× em `tests/`** (2 destas com `random_state=42` copiado no lugar de `config.SEED`). Consolidar num `src/modelos.py` — mais a renomeação de `src` → nome próprio herdada da A05/item 75. ⚠️ **Não é agenda:** a API carrega artefato, não instancia estimador. A parte com consequência está isolada no item 78 | Fase 1, 9.5 | ⬜ |
| 5 | M04 (a estudar) | FastAPI a fundo → construir a API só depois deste módulo | 9 | ⬜ |
| 6 | M05 (a estudar) | Empacotamento e SDK → como distribuir o pipeline como biblioteca | 9, 11 | ⬜ |
| 7 | M01-A08 (§1 EDA) | Testar `Latitude`/`Longitude` como **experimento controlado**: performance E disparidade, com e sem. Escapam da cardinalidade (são contínuas), não da objeção ética | 4, 10.5 | ⬜ |
| 8 | M01-A08 (§1 EDA) | Investigar **por que fibra ótica churna 41,9% × DSL 19,0%** — contraintuitivo para o produto premium. Hipótese a testar: preço x expectativa de qualidade | 1, 4 | ⬜ |
| 9 | M01-A08 (§1 EDA) | Destino de `Total Charges` (correlaciona **0,9996** com `Tenure × Monthly`). 🔺 **Terceira evidência independente na Etapa 7:** a L1 pura da configuração 1-SE zerou o coeficiente dela **inteiro** — somando-se à correlação (EDA) e ao 7º lugar na `permutation_importance` (Etapa 6, contra 2º do MDI). Três métodos sem mecanismo em comum, mesma conclusão. **Decisão: mantida no contrato v1**, porque remover não muda métrica (CV 0,6883 × 0,6904) e o custo de coleta de uma coluna já faturada é nulo. Reabrir se a Etapa 9 mostrar custo de schema | 5, 7 | 🟡 |
| 10 | Enunciado FIAP | ~~`pyproject.toml` + `Makefile`~~ → **FEITOS na Etapa 9.5**. Resta o **vídeo de 5 min (método STAR), que vale 10%** — o mesmo peso de toda a documentação, e não é código | 11 | 🟡 |
| 11 | M01-A08 (§2) | 🚨 O arquivo bruto está **ordenado pelo alvo** → o "subset minúsculo" do CI leve **tem de ser estratificado**, senão o treino recebe uma classe só e o erro do sklearn não aponta a causa | 9.5 | ⬜ |
| 12 | M01-A08 (§3) | Congelar as **estatísticas de referência do treino** (`describe()` + frequências) junto do artefato — sem baseline não há drift a detectar depois | 9, 10 | ⬜ |
| 13 | M01-A08 (§3) | O limiar de operação (**0,22** no baseline) é parâmetro de negócio: precisa ser **configurável na API**, não constante no código | 9 | ⬜ |
| 14 | M01-A08 (§5) | ~~Repetir a comparação com **validação cruzada estratificada**~~ → **FEITO na Etapa 6**: CV repetida 5×3 no treino. Foi ela que revelou o empate técnico (3 modelos dentro de 0,0033 contra desvio de 0,0188–0,0236) | 6 | ✅ |
| 15 | M02-A04 (§5c) | ~~Calibração (Platt/isotônica)~~ → **MEDIDO na Etapa 6 e dispensado por ora**: Brier praticamente idêntico entre as três famílias (0,1339 LogReg · 0,1343 RF · 0,1320 HGB). A compressão prevista da RF **não ocorreu** com `min_samples_leaf=5` + 300 árvores — hipótese refutada no mecanismo. Reabrir se a Etapa 7 mudar muito a configuração | 6, 7 | ✅ |
| 16 | M01-A08 (§3 Etapa 4) | ~~Reavaliar as 4 features com o **MLP**~~ → **FEITO: +0,0006 (0,03 dp), nulo.** Terceira família de modelos a rejeitá-las (linear, árvore, rede). A expectativa registrada estava certa, e o argumento fica melhor invertido: *"a rede aprende interação sozinha"* corta nos dois sentidos — se a interação valesse algo, ela a acharia **sem** a feature pronta | 8 | ✅ |
| 17 | M01-A08 (§3 Etapa 4) · **medido pela M03-A03** | 🚨 O `FunctionTransformer` acopla o artefato ao código-fonte (`skops_trusted_types`). Se alguma FE entrar no modelo final, o **Dockerfile e a API precisam ter `src.features` importável no mesmo caminho**. 🔺 **MEDIDO em 12/08/2026, com N=9:** carregando cada artefato do `mlruns/` num processo com a raiz do repo **fora do `sys.path`**, **8 dos 9 carregam** e **1 falha** — `logreg+feat` (`ModuleNotFoundError: No module named 'src'`), o único com o `FunctionTransformer`. Duas conclusões: (a) o acoplamento é **real e reproduzível**, não teórico; (b) **o campeão está do lado seguro**, porque nenhuma FE entrou nele — o Dockerfile não precisa de `src/features.py` por esse motivo. ⚠️ **Continua aberto** porque o `mlruns/` guarda os dois tipos de artefato e **nada os distingue**: apontar para o run errado quebra o container na inicialização. Fecha com o teste do item 66 | 9 | 🟡 |
| 18 | M01-A08 (§3 Etapa 4) | ~~RF 0,6878 × LogReg 0,6868~~ → **CONFIRMADO como empate na Etapa 6** com CV repetida: LogReg 0,6904 · HGB 0,6919 · RF 0,6886, todos dentro de 0,07 dp. E descoberto que **a regularização move mais o número que a família** (RF: 0,6519 → 0,6886 só com `min_samples_leaf=5`) | 6 | ✅ |
| 19 | M02-A01 (§2) | *(opcional — "se sobrar tempo")* Detector de drift **multivariado** com `IsolationForest` ao lado do PSI. Data drift **é** drift de `P(X)`, o objeto do não supervisionado; KS/qui²/PSI olham **uma coluna por vez** e são cegos ao caso em que só a **combinação** virou inédita (`tenure` alto **com** contrato mensal). O PSI já cumpre o requisito — isto é diferencial, não obrigação | 10 | ⬜ |
| 20 | M02-A01 (§8) | Escrever no decision log a frase de defesa do **"por que supervisionado?"**: o rótulo `Churn` já existe no histórico e a pergunta de negócio é `P(cancelar \| perfil)` → o objeto a estimar é `P(y\|X)`. Pergunta de abertura previsível da banca | 0, 11 | ⬜ |
| 21 | M02-A02 (§1) | 🔑 **Registrar no decision log** que `LogisticRegression()` traz `penalty='l2'`, `C=1.0` por default → o baseline **já é uma Ridge**, e é essa L2 que torna o `OneHotEncoder` **sem `drop`** seguro (dummies somam 1 = intercepto → `XᵀX` singular em OLS puro). Resposta pronta para *"não tem dummy trap aí?"*. Listar `get_params()` efetivos, não os digitados | 2, 3, 11 | ⬜ |
| 22 | M02-A02 (§2) | ~~Etapa 7: `C` em escala logarítmica + `penalty` como 2º eixo~~ → **FEITO, e o resultado é ganho ZERO**: `C=1,0` com L2 (o default) **já era o pico** das 18 configurações (0,6904 — o mesmo número da Etapa 6). A expectativa registrada antes de medir (`C` alto, ganho pequeno com `n >> p`) confirmou-se na forma mais forte possível. 🔺 Achado colateral: o eixo `penalty` **não existe mais** — o sklearn 1.8 o depreciou e unificou em `l1_ratio`, o que trouxe o elastic net de graça | 7 | ✅ |
| 23 | M02-A02 (§3) | Etapa 9: `/health` vira **prontidão** (readiness), não só vitalidade — valida artefato carregado + **schema de features idêntico ao do treino** + versão. É onde o item **17** é verificado em runtime. 🔺 **M03-A02 deu o caso nomeado e uma exigência a mais:** **Knight Capital (2012)** — código novo em 7 dos 8 servidores, o oitavo com legado reativado por flag reaproveitada, **US$ 440 mi em 45 min**. Não é caso de "faltou teste": é ***o que estava servindo não era o que tinha sido validado, e nada perguntou***. Logo o `/health` tem de **declarar QUAL modelo está carregado** (`run_id`/`sha256` + `model_version` + hash do schema), o mesmo `model_version` tem de ir na resposta do `/predict` (senão o log da Etapa 10 não sabe qual modelo gerou cada linha) e a API tem de **falhar na inicialização** se não bater. Health que responde 200 sem identificar versão **é o servidor nº 8**. Até aqui o item estava justificado só por analogia com as probes do k8s — agora tem caso citável na Etapa 11 | 9, 10, 11 | ⬜ |
| 24 | M02-A02 (§5) | *(condicional)* Se a documentação for mostrar **coeficientes** para explicar a priorização, rodar **VIF** antes: multicolinearidade não derruba PR-AUC, mas infla erro-padrão e **inverte sinal**. Suspeitos: `Contract` (domina, 39,9 pp) e a redundância `Electronic check` × mês-a-mês (78%) | 11 | ⬜ |
| 25 | M02-A03 (§2) | 🎯 **Tabela de odds ratios** (`np.exp(coef_)`) das 13 features na documentação/vídeo — coeficiente cru não comunica, `exp(β)` sim ("mês-a-mês multiplica por X as chances"). Duas ressalvas em rodapé: **odds ratio ≠ risco relativo** (divergem com prevalência 26,5%) e coeficientes **encolhidos pela L2** ⇒ conservadores. Artefato mais barato/persuasivo da entrega | 11 | ⬜ |
| 26 | M02-A03 (§3) | ~~Usar o argumento **teórico** na abertura das Etapas 6 e 8~~ → **USADO nas duas, e o contrafactual se realizou quatro vezes** (Etapas 4, 6, 7 e 8). Segue para a Etapa 11 como argumento de documentação, não como pendência de execução. Original:: a LogReg supõe **linearidade no log-odds** e é estruturalmente incapaz de efeito não monotônico; árvores e MLP não têm essa restrição. Melhor que "é exigência da fase". Contrafactual também vale: se os não-lineares não ganharem, o achado é *"o sinal é essencialmente linear no logit"* | 6, 8, 11 | ⬜ |
| 27 | M02-A03 (§4) | ~~Etapa 8: usar **`BCEWithLogitsLoss`**~~ → **FEITO, com teste que trava a decisão** (`test_hidden_vazio_e_uma_unica_camada_linear` verifica que não há `Sigmoid` no fim da rede). A igualdade de perda é o que sustenta o experimento controlado: entre LogReg e MLP muda **só a profundidade**. Segue para a Etapa 11 como argumento escrito | 8, 11 | ✅ |
| 28 | M02-A03 (§5) | ~~Conferir convergência do LBFGS~~ → **RESOLVIDO DE FORMA PERMANENTE na Etapa 9.5**: `filterwarnings = ["error::sklearn.exceptions.ConvergenceWarning"]` no `pyproject.toml` converte o warning em **erro de teste**. Melhor que a inspeção manual de `n_iter_`, que alguém esquece de repetir | 3, 9.5 | ✅ |
| 29 | M02-A04 (§5a) | ~~Encoding por família de modelo~~ → **TESTADO na Etapa 6, HIPÓTESE REFUTADA**: efeito ≤0,35 dp e **muda de sinal em 2 dos 5 pares**. Causa: as 10 categóricas do Telco têm 2–3 níveis, e com 2 níveis one-hot e ordinal são literalmente a mesma coluna. O pedágio previsto existe, mas só com cardinalidade alta. Código dos dois encodings mantido (`encoding=`), decisão não adotada | 2, 6 | ✅ |
| 30 | M02-A04 (§5b) | ~~`permutation_importance` em vez de MDI~~ → **FEITO, e o viés ficou provado nos dados**: o MDI põe `Total Charges` em **2º** e a permutação em **7º**; `Contract` sobe de 4º para 2º na permutação, concordando com a EDA (39,9 pp). A documentação usa permutação; o MDI fica como demonstração do viés | 6, 11 | ✅ |
| 31 | M02-A04 (§5c) | ~~Re-derivar o limiar por modelo~~ → **FEITO e quantificado em reais**: limiares ótimos 0,29 (LogReg) · 0,27 (RF) · 0,22 (HGB). Herdar o 0,22 na RF custaria **+R$ 1.090/ciclo** sem sintoma visível (PR-AUC não muda, só o volume da fila). Reforça o item 13 (limiar configurável na API) | 3, 6, 9 | ✅ |
| 32 | M02-A04 (§7) | ~~🎯 **Escrever a expectativa ANTES de rodar a Etapa 8**~~ → **FEITO: 6 previsões com critério de refutação, placar 5 acertos e 1 erro.** A previsão principal (o MLP não vence) confirmou-se: −0,0031 na validação. E a **refutada** rendeu mais que as certas — produziu o controle de orçamento que decompôs a diferença em dados (−0,0014) e otimizador (−0,0053). Original: o material (pág. 23) afirma que árvores são estado da arte em dados tabulares, *"superando inclusive redes neurais profundas"* ⇒ a previsão é que o **GBM vença o MLP**. Previsão registrada e confirmada vale mais na banca que vitória por sorte. Fecha com o contrafactual da M02-A03 | 8, 11 | ⬜ |

| 49 | Execução 7 | 🔺 **A quarta medição chegou: a 1-SE do MLP desceu à profundidade ZERO.** São quatro instrumentos sem mecanismo em comum (FE, gap viés-variância, busca em boosting, busca em rede) dizendo o mesmo. Vira parágrafo da Etapa 11, não pendência de execução. Original: (`max_leaf_nodes=2`, o toco de decisão) mesmo depois de estendida de 4 para 2. Não há "menos que 2", então o eixo está esgotado — mas a leitura vale registro: um modelo-base de duas folhas não representa interação nenhuma, é um aditivo de decisões univariadas. Se a Etapa 8 confirmar que o MLP também não ganha, são **quatro** medições independentes de que o sinal do Telco é essencialmente aditivo/linear | 8, 11 | ⬜ |
| 50 | Execução 7 | **A configuração 1-SE existe medida mas não adotada** (`C=0,1`, L1, 8 dummies zeradas, 2 colunas originais inteiras). Se a Etapa 9 mostrar que o schema/latência da API cobra caro por coluna, ou se a Etapa 10.5 decidir remover `Gender`, ela volta a ser a candidata natural — o número dela na validação já está medido (0,6620) e não precisa ser re-rodado | 9, 10.5 | ⬜ |
| 51 | Execução 7 | 🎯 **Material de vídeo quase pronto, e barato:** o diagnóstico gráfico da M02-A04 (curva treino × validação vs. número de árvores) nunca foi plotado — o `early_stopping` do HGB o resolve por dentro (115 árvores de 1.000). Uma figura com a inflexão do boosting ao lado da estabilização da RF explica em 15 segundos por que `n_estimators` regulariza num e não no outro | 11 | ⬜ |

| 52 | Execução 8 | 🎯 **O MLP ganhou no custo em reais e perdeu na métrica de seleção** — R$ 31.271 × R$ 31.750 do campeão (−R$ 479/ciclo, −1,5%), com dispersão entre suas próprias seeds de apenas R$ 203. A decisão seguiu a hierarquia declarada na Etapa 0 (PR-AUC seleciona), e o recall@10% empata exato (0,278). **Reabrir se a Etapa 9/10 mostrar que o critério de negócio é custo** — o número já está medido e não precisa ser re-rodado. ⚠️ Ler junto a ressalva: o custo mínimo usa um limiar escolhido na própria validação, para os dois modelos | 8, 9, 11 | ⬜ |
| 53 | Execução 8 | **O artefato da rede não foi serializado** — a Etapa 8 mede e reporta, mas não salva `.pt` nem loga o modelo no MLflow (só métricas e a figura). Como o modelo servido é a LogReg, isso não bloqueia a Etapa 9; mas se a banca quiser o MLP carregável, é preciso decidir entre `state_dict` + código da classe ou `mlflow.pytorch.log_model` — e o segundo tem o mesmo acoplamento ao código-fonte do item 17 | 8, 9 | ⬜ |
| 54 | Execução 8 | ⚠️ **O CI instala ~800 MB de `torch` em dois jobs para um modelo que não vai a produção.** Com a decisão da Etapa 8 (campeão = LogReg), `torch` deixou de ser dependência de runtime e virou dependência de *experimento*. Junta-se ao item 33: um `requirements-ci.txt` sem torch/shap/fairlearn cortaria a maior parte dos 7min30s do run | 9.5 | ⬜ |
| 55 | Execução 8 | 🎯 **Figura pronta e ainda não usada:** `data/processed/etapa8_curva_treino.png` — perda de treino × validação interna com a linha do early stopping, ao lado da curva de PR-AUC por época. É o diagnóstico gráfico que o item 51 pedia para o boosting, entregue para a rede. Duas figuras irmãs no vídeo explicam em 15 segundos o que é overfitting e por que o critério de parada é uma escolha de métrica | 11 | ⬜ |

| 56 | M02-A08 (§4e) | 🚨 **O plano de monitoramento tem UM limiar de alerta, e um limiar único oscila em torno de si mesmo.** A aula (pág. 15) dá a forma de termostato: *"retreine somente se o erro ultrapassar 5% por 3 dias consecutivos; mas só reverta se cair abaixo de 3% — prevenindo ficar retrain on/off caso oscile em torno de 4-5%"*. Escrever **dois** limiares (gatilho e desarme) + persistência temporal na tabela da Etapa 10, com a ação de cada um. O limiar de desarme é o que impede o *retrain on/off*, que é a fadiga de alerta na versão caríssima (cada disparo gasta um ciclo de retreino, não só atenção). 🔗 **É a mesma histerese da regra 1-SE** aplicada ao tempo em vez da complexidade — escrever a simetria mostra princípio, não receita | 10, 11 | ⬜ |
| 57 | M02-A08 (§4h) | **Alternativa arquitetural a registrar e rejeitar com razão: modelos especializados por subgrupo.** A aula (pág. 23) descreve o padrão (*"se cliente autônomo, use modelo X, caso contrário Y"*) e o TC tem o gancho medido na EDA: churn **41,7% entre idosos × 23,6% entre não-idosos**. Dois argumentos para rejeitar, e vale escrever os dois: (a) **estatístico** — o subgrupo é pequeno demais para sustentar modelo próprio, e a segmentação refaz o problema de variância que a Etapa 6 acabou de medir; (b) **governança** — condicionar o modelo a atributo sensível é *disparate treatment*, o oposto do que a M01-A07 pede. Enriquece a §5 do decision log com uma alternativa não trivial, sem custar execução | 11 | ⬜ |
| 58 | M02-A08 (§1) | 🎯 **Controle positivo do MLP — medido, falta escrever.** `moons-controle-positivo.py` (em `~/pos-mle/disciplinas/fase-01/02-fundamentos-modelos-ml/`) roda **o mesmo desenho da Etapa 8** (profundidade 0 × 1, `BCELoss`, Adam) no `make_moons`: **+9,93 pp ± 5,20**, rede ganha em 26/30 seeds — contra **−0,31 pp** de PR-AUC no Telco. Converte a §5d de *"a rede não superou"* em *"o experimento detecta 10 pontos onde há estrutura não-linear, e não achou nada aqui"*. **5ª medição** de "sinal linear no logit" e a primeira com controle positivo. Custo: colar dois números na documentação | 8, 11 | ⬜ |
| 59 | M02-A08 (§4d) | **λ implícito — tornar a hierarquia de métricas auditável.** A aula propõe escalarizar (`J = Acurácia − λ × Custo`); rejeitamos porque λ escolhido depois de ver o resultado é o `argmax` um andar acima (item 52). Mas **dividir uma métrica pela outra dá o preço que a decisão já está cobrando**: R$ 479 ÷ 0,0031 ⇒ manter o campeão equivale a declarar que **0,01 de PR-AUC vale mais de R$ 1.545 por ciclo**. Uma linha na §6, e a decisão deixa de ser dogmática | 11 | ⬜ |
| 60 | M02-A08 (§4a–4c) | **Três citações de custo zero para a Etapa 11**, todas do material oficial: (a) **pág. 17** descreve a metodologia como *"20% como teste final e não tocar durante as comparações, 5-fold CV nos 80% de treino"* = literalmente o nosso 60/20/20 + CV no treino ⇒ o protocolo passa a ser **conformidade**, não escolha; os 4 itens do "padrão operacional" servem de **índice** da seção de comparação. (b) **pág. 23** nomeia o `decision-log.md`: *"registro de decisões"* / *"dossiê do processo: métricas, gráficos, testes realizados, considerações de custo"*. (c) **pág. 22** defende o código mantido e não usado (*"o perdedor fica guardado — reprodutibilidade e aprendizado organizacional"*) ⇒ justifica `mlp.py`/`comparacao.py`/`tuning.py`/`--features` no repo, **com a ressalva de que código ≠ dependência** (ver itens 33/54). ⚠️ O critério de exemplo da pág. 17 é **relativo** (*"≥ 2 pontos acima do atual"*) — manter a justificativa do piso absoluto | 11 | ⬜ |

| 61 | M03-A01 (§3a) | 🎯 **Citação de peso para a Etapa 11, e responde a pergunta que a banca faz:** *"por que tanta infraestrutura para uma regressão logística?"* **Sculley et al. (2015), na bibliografia oficial da aula** — em sistemas de ML maduros *"o código de aprendizado é menos de 5% do total; 95% é infraestrutura e código auxiliar"*, e a dívida técnica de ML é **mais insidiosa** porque há três artefatos (código, dados, modelo) em vez de um. No repo a proporção confere: `train.py` = 185 de ~2.900 linhas. Escrever junto o termo **"glue code"**. Custo: um parágrafo | 11 | ⬜ |
| 62 | M03-A01 (§3b) | **O gate do CI ganha nome de prática consagrada.** A aula (pág. 25) descreve **teste de regressão para ciência de dados**: *"salvar resultados de referência (métricas de um modelo atual) e ter testes que alertem se um novo modelo piorou drasticamente — assim você evita deployar sem perceber um modelo com desempenho inferior"*. É o job `gate` escrito antes da aula ⇒ vira **conformidade citável**. ⚠️ Escrever a diferença a nosso favor: a aula dá critério **relativo e vago** (*"drasticamente"*); o nosso é **absoluto e de dois eixos** (PR-AUC ≥ 0,66 **e** Brier ≤ 0,14), pela razão já registrada na 9.5 | 11 | ⬜ |
| 63 | M03-A01 (§5) | **Critério explícito para a §5 do decision log: "código morto" × "registro de experimento".** Esta aula manda **remover código morto** como forma de pagar dívida técnica; a M02-A08 (item 60c) manda **guardar o modelo perdedor**. A fronteira é o próprio decision log: código que sustenta uma decisão documentada não é morto — código que ninguém sabe por que existe, é. Uma frase resolve a tensão e mostra critério em vez de acúmulo. ⚠️ Ler junto com 33/54: **código ≠ dependência**. 🔺 **A M03-A03 (pág. 29) dá o caso nomeado que fecha a fronteira:** a leitura dela do Knight Capital é *"faltou teste cobrindo o cenário de **código legado não utilizado**"* — e o que matou a empresa não foi guardar código velho, foi ele continuar **implantado e acionável por uma flag**. Logo: ✅ o perdedor fica no histórico e no decision log; 🚨 o perdedor **não fica no artefato implantável** — nada além do campeão pode ser alcançável pelo processo que serve `/predict`, nem por flag, nem por variável de ambiente, nem por um `if modelo == "mlp"` esquecido. Escrever assim, com o caso citado, é mais forte que a frase genérica | 11 | ⬜ |
| 64 | M03-A02 (§2) | 🎯 **A estratégia de versionamento do repo ganha nome e precedente publicado.** Fato medido: **20 commits, zero branches além da `main`, zero merges** ⇒ o repo é **trunk-based puro**, a coluna direita da Tabela 1 da aula (pág. 13–14). Até aqui isso era "convenção do histórico"; escrever como decisão custa um parágrafo e vem com o argumento fechado, porque **o pré-requisito que a própria aula aponta como o obstáculo do TBD está atendido e é verificável**: *"altíssima confiança em testes automatizados"* = 54 testes, `make ci` idêntico ao CI, workflow disparando em `push` **e** `pull_request`, gate de dois eixos **verificado reprovando**. ⚠️ **Escrever junto a ressalva honesta:** com um autor e sem branch protegida, o gate roda **depois** do push — quem fecha a fresta hoje é o `make ci` local, não a plataforma. Declarar a diferença entre o nosso arranjo e o TBD corporativo é mais forte que omitir. Se o grupo passar a commitar, o que se liga primeiro é **branch protegida + "sem merge com teste falhando"** (30 segundos de configuração, 90% do benefício) | 11 | ⬜ |
| 65 | M03-A02 (§3) | **A ausência do DVC deixa de ser buraco e vira decisão documentada.** O enunciado da M01-A08 sugeria DVC e não usamos; a aula (pág. 10) dá o critério em uma frase — *"Git para código e pequenos arquivos de configuração; DVC ou outros para dados brutos e artefatos volumosos"* — e **os sete artefatos do repo conferem com ele**: `data/raw/*.xlsx` (1,3 MB, **imutável** ⇒ versiona uma vez) no Git; `data/processed/` (derivável), `models/` (*"o Registry é a fonte de verdade"*), `mlruns/`, `logs/` + `*.jsonl` (**LGPD** — vai além do que a aula pede) e `.env`/`*.pem` no `.gitignore`. E a linhagem que o DVC daria já vem de **`sha256_dataset()` + `commit_hash()`** logados no MLflow, que é literalmente o *"saber qual versão dos dados foi usada em cada commit"* da aula. Um parágrafo converte omissão aparente em aplicação do critério | 11 | ⬜ |

| 66 | M03-A03 (§2) | 🎯 **Dois testes de artefato para a Etapa 9 — a suíte de 54 testes nunca tocou o objeto serializado.** Tudo o que ela exercita é o pipeline **em memória**; a serialização, os `skops_trusted_types` e a disponibilidade do código-fonte no ambiente de destino ficam sem cobertura, e a primeira execução real acontece no container. A pág. 27 pede exatamente os dois: (a) **round-trip** — treinar → salvar → limpar → recarregar → predições **bit a bit** (verificado à mão em 12/08: `max |dif| = 0.0`, mas nada garante que continue); (b) **carga sem `src` no `sys.path`**, em subprocesso, que é o cenário do container — foi esse que revelou o artefato quebrado do item 17. Custo: ~15 linhas. É o teste com maior razão achado/linha de toda a etapa → ✅ **FEITO em 17/08/2026 na Etapa 9c** (`tests/test_artefato.py`): os dois testes pedidos **mais três** que a implementação revelou serem necessários — versão de sklearn forjada é barrada, formato desconhecido é barrado, e metadados ⇄ `feature_names_in_` não podem divergir. O round-trip roda **também no ato da promoção** (`src/promover.py`), não só na suíte: promover sem reler é a mesma fé do `/health` que só prova que o processo subiu | 9 | ✅ |
| 67 | M03-A03 (§3) | **Contrato de erro + smoke test da API.** `pytest.raises`/422 para os quatro payloads inválidos (campo faltando · tipo errado · **categoria inédita** · limiar fora de [0,1]) — com `dependency_overrides` (9d-quater) esses testes rodam **sem carregar modelo**, então são unitários de verdade e rápidos. Mais o **smoke test** `dry_run=True` (pág. 28), que percorre a inicialização inteira sem servir requisição: não valida predição, valida que o **ambiente do container** está de pé. Junto com eles, o teste que verifica que o `run_id` do `/health` **é o do campeão registrado** — identidade sem teste é comentário (9d-quinquies) → ✅ **FEITO em 17/08/2026 (9d):** os quatro payloads dão 422 em `tests/test_api.py`, com `dependency_overrides` para a lógica e o artefato real para o contrato. Mais o que a execução acrescentou: o teste que embaralha as chaves do JSON, o que verifica que o 500 não vaza `str(e)` e o que confere que o `/health` declara o **sha256 do artefato** — a identidade virou teste, não comentário. Smoke test real: a API foi exercitada com **uvicorn**, não só com `TestClient` | 9 | ✅ |
| 68 | M03-A03 (§4) | 🎯 **`importorskip` fecha os itens 33/54 sem apagar código.** `torch = pytest.importorskip("torch")` em `tests/test_mlp.py` + `requirements-ci.txt` sem torch/shap/fairlearn no job `qa` ⇒ os 12 testes de MLP são **pulados com relatório**, não removidos, e voltam a rodar em qualquer ambiente completo. Preserva a regra da M02-A08 (*"o perdedor fica guardado"*) e materializa a distinção que o item 63 pedia: **código ≠ dependência**. ⚠️ `--strict-markers` está ligado — marker novo tem de ser declarado em `[tool.pytest.ini_options] markers`. Custo: 2 linhas + um arquivo | 9.5 | ⬜ |
| 69 | M03-A03 (§1) | *(Etapa 8, fechada — não refazer agora)* **Teste de sanidade "o modelo consegue overfitar 10 amostras?"** (pág. 25–26, prática relatada por engenheiros do Google). Se a rede não consegue memorizar um lote minúsculo, o bug é na **função de treino** (gradiente, atualização de pesos), não nos dados nem na arquitetura. O `src/mlp.py` tem 12 testes e nenhum deles falsifica isso. É o irmão barato do controle positivo do `make_moons` (item 58): um testa **a implementação**, o outro testa **o experimento** — e a Etapa 8 gastou uma decomposição inteira (otimizador × early stopping) que teria começado mais rápido com ele | 8 | ⬜ |
| 70 | M03-A04 (§2b) | 🚨 **`.dockerignore` ANTES do Dockerfile — `.gitignore` não é lido pelo Docker.** Medido em 12/08/2026: um `COPY . .` neste repo copia **1,5 GB** (o `.venv`, com binários **arm64 de macOS** indo para uma imagem Linux); sem `.venv`/`.git` o repo inteiro são 6,6 MB. Mas o problema não é peso: `mlruns/` (os 9 artefatos, incluindo o `logreg+feat` quebrado do item 17), `mlflow.db`, `data/raw` (Telco, 7.043 clientes) e os futuros `logs/*.jsonl` estão todos no `.gitignore` — **marcados LGPD lá mesmo** — e entrariam na imagem. **Camada Docker é imutável:** `RUN rm` posterior não apaga da camada anterior; quem puxar a imagem tem o dado. A disciplina de exclusão do repo existe e está certa, só mora no arquivo errado para este ambiente | 9 | ⬜ |
| 71 | M03-A04 (§2a, §2c, §3) | **A base da aula não builda este projeto, e a plataforma padrão está errada.** (a) `FROM python:3.9-slim` falha: `scikit-learn 1.9.0` e `numpy 2.4.6` exigem `>=3.11` (venv em 3.12.5) ⇒ usar **`python:3.12-slim` fixado por `@sha256:`**, porque tag é mutável. ⚠️ A falha alta só existe **porque há pins** — sem eles o pip resolveria versões antigas, a imagem construiria e o artefato skops quebraria só na **carga dentro do container**: a versão do Python é parte do contrato do artefato, mesma classe do item 17. (b) 🚨 Build no Mac (Apple Silicon) produz `linux/arm64`; runner do Actions e cloud são `x86_64` ⇒ `exec format error` **no deploy**, não no build local que passou. Usar `docker buildx build --platform linux/amd64` e declarar no README. (c) Ordem das camadas: `COPY requirements.txt` → `RUN pip install` → `COPY src/`, senão cada linha de código reinstala o mundo | 9 | ⬜ |
| 72 | M03-A04 (§4) | **Imagem de serviço ≠ imagem de treino — o irmão em runtime dos itens 33/54.** O campeão é a LogReg, logo a imagem que serve `/predict` não precisa de `torch`, `shap`, `fairlearn`, `matplotlib` nem do mlflow completo. Um `requirements-serve.txt` materializa a mesma distinção **código ≠ dependência** do item 63/68, agora do lado do runtime — e é o que separa uma imagem de ~200 MB de uma de mais de 1 GB. Junto: `USER` não-root e `HEALTHCHECK` apontando para o `/health` de **prontidão** (9d-ter), não de vitalidade | 9 | ⬜ |
| 73 | M03-A04 (§4) | 🎯 **Falta o terceiro artefato de Sculley: o ambiente.** O repo já loga `commit_hash()` (código) e `sha256_dataset()` (dados) no MLflow; logar o **digest da imagem** que treinou custa uma linha e fecha a tríade **código · dados · ambiente**, transformando a provocação *"alguém consegue reproduzir seu número exato?"* em resposta verificável. É também o argumento da Etapa 11 que a aula sustenta melhor: *o Dockerfile é a receita, a imagem é o artefato* — só a segunda é reprodutível por construção | 9.5, 11 | ⬜ |

| 74 | M03-A05 (§2a) | 🚨 **Não construir a imagem de serviço a partir do `requirements.txt` que o MLflow gravou no artefato.** Medido em 12/08/2026 nos **9 artefatos**: o snapshot é **idêntico nos nove** (`mlflow, numpy, pandas, pytest, scikit-learn, scipy, skops, torch, tqdm`), incluindo `torch==2.13.0` (**523,6 MB**) e `pytest` (dependência de *teste*) no snapshot da **LogReg campeã**. Motivo: o MLflow infere requisitos dos **módulos importados pelo processo que logou**, não do objeto serializado — 🔑 *inventaria o que o processo importou, não o que o modelo precisa*. E erra também **por omissão**: `src` não aparece em nenhum dos 9, embora o `logreg+feat` (item 17) não carregue sem ele. Consequência prática: `mlflow models build-docker`/`serve`, que é o caminho grátis, instala PyTorch para servir uma regressão logística. A aula (pág. 13) apresenta esse arquivo como garantia — é subproduto | 9, 11 | ⬜ |
| 75 | M03-A05 (§2c) | 🎯 **A correção de RAIZ do item 17: `pip install .` no Dockerfile.** O `pyproject.toml` **já** tem `[build-system]` e `packages = ["src"]` — mas `pip show tc-mle-fase1` = *not found*, e `import src` com cwd fora do repo dá `ModuleNotFoundError` (medido; após instalar, resolve pelo `site-packages`). Com o pacote instalado, o `FunctionTransformer` carrega **independentemente de `WORKDIR`, cwd e `PYTHONPATH`**. Soma-se ao item 70, não compete: instalar o pacote **e** não deixar `mlruns/` entrar na imagem. ⚠️ **Duas armadilhas:** (a) `COPY . .` **+** `pip install .` deixa **duas cópias** do código na imagem (`/app/src` e `site-packages/src`) e quem vence depende da ordem do `sys.path` — escolher uma; (b) `packages = ["src"]` publica um pacote de topo chamado **`src`** (distribuição `tc_mle_fase1`, import `src`), nome genérico que colide entre projetos — renomear é trabalho do **item 4 / M05**, não de agora. Os testes escapam disso porque `tests/__init__.py` faz o pytest inserir a raiz no `sys.path` (verificado rodando a suíte de fora do repo) | 9 | ⬜ |
| 76 | M03-A05 (§2d) | 🚨 **Trocar `pip freeze` por `pip-compile` ANTES de empacotar — senão o próprio pacote envenena o lockfile.** O procedimento documentado no `requirements.in` é `pip install -r requirements.in && pip freeze > requirements.txt`. Hoje o lock está limpo (verificado: sem `-e`, sem `file:///`) **porque o projeto não está instalado**. No instante em que o item 75 acontecer, o `pip freeze` passa a emitir — medido nas duas formas — `-e git+https://github.com/…/tc-mle-fase1.git@<SHA>#egg=tc_mle_fase1` (editable; o remoto é **privado** ⇒ `pip install` no Docker/CI falha por **credencial de git**) ou `tc-mle-fase1 @ file:///Users/lucastephan/tc-mle-fase1` (não-editable; caminho absoluto da máquina). A falha aparece no lugar mais confuso: `docker build` de um Dockerfile correto, erro de auth de git para o próprio projeto. `pip-compile` (pip-tools, citado na pág. 9 da aula) resolve transitivas **e** exclui o próprio projeto; `pip freeze --exclude-editable` só cobre metade. Junto: `build/` e `dist/` no `.gitignore` (`*.egg-info/` já está) | 9.5 | ⬜ |
| 77 | M03-A05 (§2b, §2e, §5, §3) | **Quatro correções de baixo custo, medidas.** (a) 🚨 **`skops` é dependência DIRETA não declarada:** todo artefato é `model.skops` (`serialization_format: skops` no `MLmodel`), mas ele não está no `requirements.in` e chega só como **transitiva do mlflow** (`Required-by: mlflow`). Se um mlflow futuro deixar de puxá-lo, o próximo lock sai sem skops e **nenhum artefato do histórico carrega** — uma linha resolve. (b) **A conta dos itens 33/54/72 fechada:** ambiente completo **1.497 MB / 116 distribuições**; subconjunto de serviço **265 MB** ⇒ **82% não vai para a imagem**. `llvmlite` (130,9 MB, via numba←shap) e `pyarrow` (130,6 MB, mlflow) entram como transitivas que ninguém pediu. (c) **Dependabot só DEPOIS do teste de round-trip (item 66):** subir `scikit-learn` invalida artefato serializado e **o gate não pega**, porque ele retreina antes de medir. (d) 🎯 **Etapa 11 — a frase que justifica o `pyproject` sem dependências:** *"quem é importado declara faixas; quem é implantado trava versões"* (a aula, pág. 16: *produtos querem determinismo, bibliotecas querem compatibilidade*) ⇒ o comentário defensivo do `pyproject` vira critério declarado. Com a ressalva que a aula não faz: **SemVer não promete reprodutibilidade numérica** — um MINOR de sklearn pode trocar um default sem quebrar API e ainda assim mudar o número | 9, 9.5, 11 | ⬜ |
| 78 | M03-A06 (§2b) | 🎯 **`train.construir_modelo` deve DELEGAR ao `comparacao.catalogo()` (~3 linhas).** É a fatia do item 4 que tem consequência, e ela vale sozinha. Hoje o `gate.py` treina via `construir_modelo("logreg", None)` e o artefato campeão veio de `finalistas.treinar()` → `catalogo("onehot")["logreg"]`: **duas definições independentes do mesmo objeto**, em dois módulos. Verificado em 13/08/2026 que os `get_params()` são **idênticos hoje** — o gate mede o modelo certo. O modo de falha é o dia em que uma delas ganhar um `C=0.5`: o CI aprova um modelo e o repo promove outro, **com o gate verde**, porque o gate não compara artefato, ele **retreina**. ⚠️ A divergência de `tuning.estimador_base` (`max_iter=5000`) é **deliberada e correta** (a grade vai a regiões de `C` onde o LBFGS precisa de mais iterações, e `filterwarnings = error::ConvergenceWarning` transformaria não-convergência em teste vermelho) — o problema não é ela, é que **nada distingue divergência deliberada de esquecimento** quando não há lugar único onde a diferença esteja declarada | 9.5 | ⬜ |
| 79 | M03-A06 (§2a) | ~~🚨 **Acrescentar `"RUF"` ao `select` do ruff — uma linha, e é o meta-controle.** Medido: das 3 supressões de lint do repo, **2 são inertes** — `# noqa: PLR0915` em `src/mlp.py:474` e `src/tuning.py:354` apontam para uma regra que **não está no `select`** (`E,W,F,I,UP,B,SIM,NPY,PD` — nenhum `PLR`). `ruff check . --extend-select RUF100` acusa as duas. O estrago não é o comentário morto: as duas linhas **parecem** dizer que existe controle de tamanho de função e que essas duas foram avaliadas e liberadas — e nada disso aconteceu. Prova: ligando `PLR0915`, aparecem **2 violações nunca avaliadas** (`selecao.main` 66 statements, `notebooks/01_eda.py` 68) enquanto as duas "justificadas" (127 e 81) passam a ser suprimidas de verdade. Ordem: **`RUF100` primeiro** (garante que toda supressão futura seja real), `PLR0915` depois, se quiser — custa 2 decisões. ⚠️ Método: usar **`--extend-select`**, nunca `--select`, que substitui a config do projeto e produz falso positivo (acusa o `# noqa: E731` legítimo de `tests/test_mlp.py:182`). 🔑 *Um `noqa` é uma afirmação sobre a configuração do linter, e ninguém verificava a afirmação* — mesma classe do `.gitignore` que o Docker não lê (item 70) e do snapshot do MLflow que não descreve o modelo (item 74)~~ → ✅ **FEITO em 13/08/2026, no mesmo dia.** `select` ganhou **`"RUF"`** e **`"PLR0915"`**; `ignore` ganhou **RUF001/002/003** (os `×` e `−` das strings de relatório são tipografia deliberada para leitura humana, não homoglifo em identificador — mesma lógica do NPY002 já documentado); `notebooks/*` ganhou `PLR0915` (a EDA é roteiro linear de 68 passos: quebrar esconde a ordem, que é o conteúdo); **`selecao.main` ganhou o `noqa` que lhe faltava**, agora suprimindo de fato; e **3× RUF010** corrigido (`f"{str(x)}"` → `f"{x!s}"`, fix seguro). 🔑 **Verificado REPROVANDO**, como o gate: um `# noqa: C901` injetado em `gate.py` é acusado pelo RUF100 e some ao ser removido. `make ci` verde (54 testes, gate 0,6646). Custo real: 21 linhas de config, zero de lógica | 9.5 | ✅ |
| 80 | M03-A06 (§3, §5) | **Desenho da API (Etapa 9) e um parágrafo da Etapa 11.** (a) 🎯 **SRP: rota / serviço / schema** — trocar o artefato não toca a rota, mudar o contrato HTTP não toca a predição. O ganho concreto: os **4 testes do bloco 9d-sexies rodam contra o serviço**, sem `TestClient`, e ficam rápidos. Precedente que já funcionou no repo: `gate.aprovado()` foi isolada do treino *para poder ser testada* — ali o teste puxou o desenho, não o contrário. (b) 🚨 **Nenhum `0.29` literal na API**: o limiar de operação já vive em `evaluate.curva_custo` e é parâmetro de negócio (item 13) — duplicá-lo é criar duas cópias obrigadas a mudar juntas. (c) 🚨 **Sem Strategy/Factory/Observer na API**: um endpoint de predição não passa no teste da própria aula (pág. 14 — *o benefício só aparece se as estratégias forem complexas ou numerosas*); `carregar_modelo()` com cache de módulo + middleware do FastAPI bastam. Reconhecer isso **é** o conteúdo da seção de padrões, não uma esquiva dela. (d) **Etapa 11:** **Knight Capital (2012, US$ 440 mi em 45 min por código legado sem testes)** é o único case do Módulo 03 citável — sustenta em uma frase por que existem gate, testes e CI aqui; e SRP/DRY dão **nome** ao que o repo já faz (`config.py` = DRY de configuração; `catalogo()` = Factory por dicionário de classes, exatamente a forma que a pág. 16 recomenda em Python; `gate.aprovado()` = SRP habilitando teste unitário) | 9, 11 | ⬜ |

| 81 | M03-A07 (§2a) | 🚨 **A API constrói `pd.DataFrame([payload])`, NUNCA `np.array` — e o teste que trava isso.** Medido em 13/08/2026 contra o artefato campeão (`logreg-etapa5`, val 0,6646): o padrão do hands-on da aula (`X = np.array([[f.x1,f.x2,f.x3]])`) falha com `ValueError: Specifying the columns using strings is only supported for dataframes`, porque o `preproc` é um `ColumnTransformer` que **seleciona por nome** (`feature_names_in_` = as 13 strings). 🔑 **O contra-teste é o que dá valor:** com DataFrame e as colunas **na ordem invertida**, a probabilidade sai **idêntica ao dígito** (0,6601) ⇒ *nome de coluna é contrato verificável; posição é convenção não verificada*. O `np.array([[…]])` fixa a ordem **no corpo da função**, onde nada a checa: o Pydantic valida os campos do JSON, não a sequência em que alguém os desempacota — reordenar dois atributos da classe de schema é refatoração que nenhum linter, teste ou revisor barra, e a API passa a responder **200 OK com número errado**. ⚠️ **Aqui a proteção veio de graça** (o `ColumnTransformer` foi adotado na Etapa 2 por *leakage*, não por contrato de API) ⇒ **transformar em teste**: mesma predição com as chaves do JSON embaralhadas. *Benefício que ninguém escolheu é benefício que alguém remove sem perceber* | 9 | ⬜ |
| 82 | M03-A07 (§2b) | 🎯 **`predict_proba(X)[0,1]` + limiar de config — `.predict()` custa R$ 7.546 por ciclo, medido.** O hands-on usa `model.predict(X)[0]`, que aplica **0,5 implícito** e apaga a etapa que derivou 0,29 pela curva de custo. Medido na validação (n=1.409, 374 churners, prevalência 26,54%, FN R$ 194 × FP R$ 62): **thr 0,50** → 310 flagged, TP 205, FN 169, recall 0,5481, custo **R$ 39.296**; **thr 0,29** → 531 flagged, TP 288, FN 86, recall 0,7701, custo **R$ 31.750**. Diferença: **R$ 7.546/ciclo (+23,8%) e 83 churners** que o `.predict()` perde e o limiar captura. A resposta devolve **score + decisão + o limiar aplicado** (o consumo é fila priorizada; fila precisa do score). Fecha o item 13 pelo lado do número. ⚠️ Irmão do erro, também no hands-on: `{"prediction": float(y)}` converte a **classe** para float — `0.0`/`1.0` num campo com esse nome **se lê como probabilidade**; tipo certo, significado errado, e não falha | 9 | ⬜ |
| 83 | M03-A07 (§4) | **Tabela de monitoramento da Etapa 10 no formato Golden Signals → KPI → limiar → janela → ação** (Tabela 2 da aula, podada). Entram **latência** (p95/p99 de `/predict`, alerta p99 > 300 ms por 5 min) e **erros** (5xx, > 1% por 10 min); **tráfego e saturação NÃO têm base observável** no TC (snapshot sem eixo temporal, sem carga real) e métrica supervisionada on-line idem, pela janela cega do ground truth — sobra **PSI > 0,2** como proxy de `P(X)`, que é o que o item 48 já registrou. 🔑 **A janela temporal é o que separa alerta de ruído** e é o que faltava explicitar (casa com a banda morta do 10f). Vocabulário novo para o §10 da documentação: SLI/SLO, MTTD/MTTR, e a frase citável da pág. 9 (*"a tríade traces-métricas-logs descreve o comportamento do serviço, enquanto métricas de modelo refletem a qualidade do aprendizado sob a distribuição de produção"*). 🚨 **E o achado de desenho:** blue-green comuta *"quando o green passa nos testes de fumaça"* e canary promove *"quando todos os SLIs estão verdes"* — as duas dependem de um endpoint de **prontidão**, e o hands-on **não tem `/health` nenhum** ⇒ o item 23 deixa de ser higiene e vira **pré-requisito da estratégia de release**. ⚠️ OTel/Prometheus/Grafana/cloud ficam **fora** do TC: monitoramento offline por script é o escopo certo | 10, 11 | ⬜ |
| 84 | M03-A07 (§2c, §5) | **Etapa 11 — o parágrafo que justifica o gate do repo contra o material, com número.** A aula (pág. 7) propõe promover runs com *"ΔAUC ≤ 1 p.p. ou ΔF1 ≤ 0,02"* — critério **relativo ao vigente**, cuja folga se renova a cada release. Simulado com o campeão (0,6646): release 1 aceita 0,6546 (já abaixo do piso do repo), e **cinco releases verdes chegam a 0,6146**, pior que configurações que a Etapa 6 rejeitou, **sem um único CI vermelho**. Mesmo mecanismo do *"≥80% do baseline"* do enunciado da M01-A08, recusado na Etapa 9.5. 🔑 *Gate relativo mede a última versão; gate absoluto mede o compromisso.* O gating do Facebook (*"+1 ponto sobre o vigente"*, M02-A06) é o erro **oposto** — exige melhora e congela o modelo — e nenhum dos dois substitui o piso. ⚠️ Ressalva honesta a escrever: a intenção provável do autor é *tolerância de regressão*, prática legítima em CI de software; o defeito é a **ausência da âncora absoluta ao lado**. Junto, dois créditos ao material que valem uma linha cada: o `Dockerfile` da aula acerta a **ordem das camadas** e a base (`python:3.11-slim`, ao contrário da A04), e a **tag da imagem pelo SHA do commit** é a linhagem que o M01-A06 pregou. ✅ **Nada a fazer no `ci.yml`:** o workflow da aula roda `deploy` em **qualquer branch** (`on: [push]` sem filtro, `deploy` só com `needs:`) — o repo já faz certo | 9.5, 11 | ⬜ |
| 85 | M03-A08 (§2a–c) | 🚨 **Teste de caracterização do campeão — o controle que falta ao lado do gate.** A aula (pág. 9): *"antes de modificar código crítico, escreva testes que capturam o output atual dado um input conhecido, servindo de contrato"*. O repo tem 54 testes e um gate de dois eixos, e **nenhum fixa o comportamento do campeão** — o gate é **piso** (`PR-AUC ≥ 0,66`), e piso não é contrato. **Medido em 13/08/2026** (ablação feature a feature com o gate completo): **9 das 13 features somem UMA A UMA com o CI verde**, e **5 de uma vez** (`Total Charges`, `Monthly Charges`, `Gender`, `Senior Citizen`, `Multiple Lines`) ⇒ PR-AUC 0,6660, **CI VERDE**; só a partir de 6 reprova (0,6345). Inclusive **`Contract` sozinha** (−0,0019) passa — a feature em torno da qual a narrativa do projeto foi construída. ⚠️ Não é defeito do modelo (é redundância real entre features, já vista na Etapa 5): é **ninguém ficar sabendo**. 📏 **O número que fecha:** folga do gate = 0,0046; distância entre o campeão e os **6 candidatos** que ele derrotou na Etapa 6 = 0,0057 ⇒ a folga é da ordem do intervalo inteiro em que a escolha do campeão foi decidida. ✅ **Viável:** o pipeline é **bit-determinístico** (sha256 das 1.409 probabilidades idêntico em 3 execuções; PR-AUC repr `0.6646020518504109`) — a tolerância existe para troca de biblioteca, não para ruído. ✅ **Verificado nos dois sentidos** (disciplina do item 79): `\|PR-AUC − 0,6646\| ≤ 1e-4` **reprova** as 3 mudanças que o gate aprova (5 features fora / `Contract` fora / sem escalonamento) e **passa** no controle positivo. 🔑 *Um piso mede se o modelo é bom o bastante; um contrato mede se ele é o mesmo* — controles complementares, o gate **continua** (a A07 mediu por que o piso absoluto é superior ao relativo). Note que **duas das três mudanças SOBEM a PR-AUC**: caracterização é bilateral por construção. ✅ **EXECUTADO em 13/08/2026, no mesmo dia.** `tests/test_gate.py` criado com **3 testes**; `gate.medir()` extraída de `main()` para que o CI e o teste meçam **o mesmo objeto** e não duas reconstruções (mesmo precedente de `gate.aprovado()`: o teste puxou o desenho). Referências `PR_AUC_REF=0.6646` / `BRIER_REF=0.1339`, `TOL=1e-4`, com a regra de alteração escrita ao lado (commit próprio, com justificativa — igual ao piso). **Verificado reprovando na árvore real:** removendo `Gender` do `config.CAT`, o **gate APROVA** (0,6661 ≥ 0,66 — e ainda por cima *sobe*) e a **caracterização REPROVA** (`Obtained 0.6660791395632822 / Expected 0.6646 ± 1.0e-04`). `make ci` verde: **56 testes**, 11s. | 4, 9, 9.5 | ✅ |
| 86 | M03-A08 (§2f, §2h) | **Item 4 (refatoração) — ordem de execução e dois achados de arrumação.** 🔑 **A ordem inverte:** o teste de caracterização do item 85 entra **antes** de consolidar as fábricas de estimador, não depois — sem ele a consolidação é feita no escuro (é exatamente a tese da aula: passos pequenos com testes a cada passo). **Número novo sobre o achado da A06:** `LogisticRegression(` aparece **11 vezes em 7 módulos** (`ablacao`, `comparacao`, `train`, `mlp`×3, `selecao`×2, `tuning`×2) — o mesmo achado da A06 contado só para a família do campeão. ⚠️ **A divergência `tuning.py:241` (`max_iter=5000`) NÃO é para uniformizar** — é deliberada e já justificada (regiões de `C` onde o LBFGS não converge, com `ConvergenceWarning` como erro). 🎯 **O que a A08 acrescenta é a medição do outro lado:** no campeão, `max_iter=100` (default) e `1000` dão **exatamente** o mesmo 0,6646/0,1339, porque o LBFGS converge muito antes com dados escalonados ⇒ (i) o `5000` é **específico do grid** e a consolidação tem de **preservar** a divergência com parâmetro explícito, e (ii) as outras 10 são comportamentalmente indistinguíveis, logo a dívida ali é de **manutenção**, não de resultado. 🔑 Caso didático da contra-regra do catálogo (*duplicação só é dívida quando as cópias têm de mudar juntas*): nove têm, uma tem de mudar sozinha — e é isso que uma fonte única torna visível. ✅ **A arrumação foi feita junto do item 85** (`test_gate_exige_os_dois_eixos` movido de `tests/test_mlp.py` para `tests/test_gate.py`, import órfão removido). **Falta só a consolidação das fábricas** — agora com a rede de segurança no lugar. ✅ **Não há dívida estrutural:** ruff com `C901`/`PLR0912`/`PLR0915` sobre `src`+`tests`+`notebooks` = **zero**; só 2 `PLR0913` (`MLP.__init__` 8 args, `construir_pipeline` 6), que são construtores configuráveis e não valem indireção. Nenhuma "função Deus" no repo | 4, 9.5 | ⬜ |
| 87 | M03-A08 (§3, §2e, §2g) | **Etapa 11 + duas linhas de decision log, tudo de custo baixo.** (a) 🎯 **Abertura do §10 com fonte externa de peso:** **Sculley et al., *Hidden Technical Debt in ML Systems*** (NeurIPS 2015 — *"a maior parte do sistema de ML é infraestrutura: dados, configuração, monitoramento, não o código do modelo"*) e o **Zillow Offers (2021)**, que fracassou por *"não ter sistemas robustos para monitorar e ajustar o modelo conforme o mercado mudava"*. É a justificativa externa para a Etapa 10 existir num TC cuja nota está no modelo — e a única referência de peso da bibliografia desta aula. (b) 🔑 **A distinção piso × contrato** (item 85) vale um parágrafo próprio, com os dois números (0,0046 × 0,0057). (c) **Vocabulário de catálogo para o que o repo já faz:** *Split Phase* = `data→preprocess→train→evaluate→gate`; *Encapsulate Variable* = `ColumnTransformer`; *Extract Function* = `gate.aprovado()` isolada para poder ser testada. (d) ✅ **Crédito medido, sem tarefa:** suíte (54 testes) **e** o gate end-to-end passam com `pd.options.mode.copy_on_write = True` **e** com `FutureWarning`/`DeprecationWarning` como **erro** ⇒ a meia página da aula sobre views/copies e `SettingWithCopyWarning` não morde aqui, e o repo já está pronto para o CoW virar padrão do pandas. (e) **Decision log §1 — 2 linhas:** a duplicação `notebooks/01_eda.py` ↔ `src/data.py` (`sha256`, parse de `Total Charges`) é **decisão deliberada** (a EDA registra o que foi visto *naquele momento* e não deve mudar quando `src/` evoluir), não dívida — mas hoje só existe na cabeça de quem escreveu, e pela Tabela 1 da aula lê-se como smell. ✅ O "dilema dos notebooks" já está resolvido: `01_eda.py` é **`.py`, não `.ipynb`**, com fronteira declarada na docstring | 10, 11 | ⬜ |
| 88 | M04-A01 (§2a) · ⚠️ **parcialmente corrigido pelo item 95** | 🚨 **`ConfigDict(extra="forbid")` no schema de entrada — o campo que a API ignora volta na resposta de erro.** Medido em 17/08/2026 contra **FastAPI 0.141.1 / Pydantic 2.13.4** (já no venv), app chamado por ASGI puro: payload com as features válidas **+ `"CustomerID": "3668-QPYBK"`** ⇒ **HTTP 200**, campo extra descartado em silêncio (default `extra=ignore`). E o mesmo payload sujo **com um campo obrigatório faltando** ⇒ **HTTP 422 cujo `detail[0].input` é o objeto INTEIRO**, `CustomerID` incluído. Erro de *valor* não vaza (o `input` é só o valor do campo); erro `missing` vaza tudo. ⚠️ **Por que morde aqui:** `CustomerID` está em `config.DESCARTE` como *"identificador + dado pessoal (LGPD)"* e o `.gitignore` marca `logs/*.jsonl` como LGPD (item 70) — o identificador chega ao log de acesso **sem ninguém escrever a linha que o coloca lá**. Uma linha de `model_config` fecha os dois, porque o payload deixa de poder carregar o campo. 🔑 *O campo que a API se recusa a ler é exatamente o que ela devolve de volta* — sétimo exemplar da família do M03, na forma inversa → ✅ **FECHADO em 17/08/2026 (9d)** — e com a correção do item 95 aplicada: `extra="forbid"` **mais** o handler de `RequestValidationError` que remove `input`/`ctx`. Verificado nos 8 payloads inválidos: **nenhum devolve valor do payload**, nem no caso `missing` (que ecoava o objeto inteiro) nem no `extra_forbidden` (que devolvia nome e valor) | 9, 11 | ✅ |
| 89 | M04-A01 (§2b, §2c) · ⚠️ **o (c) foi superado pelo item 94** | **O contrato de erro: item 67 CONFIRMADO por medição, e descoberto não-executável.** (a) ✅ Os quatro payloads inválidos dão **422** no FastAPI real (campo faltando · tipo errado · categoria inédita · fora de faixa) — enquanto a **Tabela 1 da aula (pág. 24) manda 400** para os dois primeiros. Seguir a tabela exigiria **sobrescrever o handler de `RequestValidationError`** contra a convenção do framework; o item 67 fica como está, agora com o número. (b) 🚨 **`httpx2` não está no `requirements.in` ⇒ `from fastapi.testclient import TestClient` levanta `RuntimeError`** — os quatro testes do item 67 e o smoke test **não rodam hoje**. Uma linha de dependência, e é a linha que separa *planejado* de *executável* (foi por isso que estas medições usaram ASGI direto). (c) 📏 **`alias` é obrigatório, não estilo:** **9 das 13 features têm espaço no nome e só 4/13 são identificadores Python válidos**. Gerar os aliases de `config.FEATURES` **ou** testar `set(aliases) == set(config.FEATURES)` — divergência de um caractere não aparece na validação, aparece como **500 no `ColumnTransformer`** (irmão do item 81). 🔑 Mesmo princípio do item 85: *duas listas mantidas iguais pela memória divergem em silêncio* → ✅ **FECHADO em 17/08/2026 (9d)**, pelo caminho do item 94 e não pelo do 89(c): os aliases são **gerados do artefato** (`pipe.feature_names_in_`), e há teste `aliases == list(feature_names_in_)`. Os 422 estão medidos e testados | 9 | ✅ |
| 90 | M04-A01 (§3, §4) | **Etapa 11 — três parágrafos prontos e uma limitação a declarar.** (a) 🎯 **Versionamento com tensão real e citável:** o tweet de **Fielding (2013)** — *"a 'v1' is a middle finger to your API customers, indicating RPC/HTTP (not REST)"* — contra a prática de mercado que a própria aula admite na página seguinte. A decisão do TC assume o `/v1` **como RPC sobre HTTP**, com a razão local: o conjunto de 13 features muda quando o modelo muda, que é o cenário de quebra que o `/v1` existe para absorver. (b) 🎯 ***"O contrato permanece enquanto o modelo por trás muda"*** (Netflix/Spotify, págs. 36–37) é o argumento externo do item 80(a) (SRP rota/serviço/schema) — e o TC tem o caso concreto: o campeão sobreviveu a **seis** candidatos dentro de **0,0057**, logo trocar o artefato é evento provável. (c) ⚠️ **Ausência de autenticação vira limitação DECLARADA**, não omissão: a aula nomeia *"API interna sem proteção"* como anti-padrão grave (pág. 33). (d) 🔑 Uma linha de processo: ***a semântica do verbo descreve o efeito no servidor, não a estabilidade da resposta*** — `POST /predict` não é idempotente por contrato embora a predição seja **bit-determinística** (M03-A08). ⚠️ **Fora do escopo, e dizer isso na banca vale mais que implementar:** GraphQL (*"canhão demais"*, palavra da aula), gRPC, SOAP, HATEOAS | 9, 11 | ⬜ |
| 91 | M04-A02 (§2a) | 🚨 **Serverless por função está descartado — com número, não por preferência.** A aula dedica três páginas a **FastAPI + Mangum + AWS Lambda** como *"a variação moderna"*, com o cenário do pico às 20h e a escala a zero às 22h, e **não menciona o limite de empacotamento**. Medido em 17/08/2026, fechamento transitivo real das dependências de serviço (`fastapi`, `uvicorn`, `pydantic`, `pandas`, `numpy`, `scikit-learn`, `joblib`): **253,2 MB / 24 distribuições** — **261,2 MB** com os extras de `uvicorn[standard]` que o `requirements.in` de fato declara — contra o limite do Lambda de **250 MB** (zip + layers, **descompactado**). **Estoura por 1,3% no mínimo teórico e 4,5% no declarado**, e isso **antes** do `mangum`, do `src` e do artefato. Culpados: `scipy` 92,9 + `pandas` 64,4 + `scikit-learn` 43,6 + `numpy` 29,8 = **230,7 MB = 91% do pacote**; o modelo campeão tem **7,4 KB**. 🔑 *O que não cabe na função serverless não é o modelo — é o que ele precisa para existir.* ⚠️ **Não é um "não" a serverless em geral:** Lambda **por imagem de container** aceita 10 GB e resolveria — mas aí é o container do 9f, e o argumento da aula (*"não se preocupar com contêineres"*) se dissolve. ✅ **Consequência: a decisão de container (M03-A04) sai confirmada por um segundo número independente, e antes da M04-6.** 📎 Números remedidos: ambiente completo **1.427,8 MB / 115 dists** (a M03-A05 registrou 265 MB / 1.497 MB / 116 dists — fechamento ligeiramente diferente; os **82% fora da imagem** se mantêm nos dois). **Usar 253,2 MB**, que é o que foi confrontado com um limite externo | 9, 11 | ⬜ |
| 92 | M04-A02 (§2b, §2d) | 🚨 **Duas medições de partida que fixam o desenho do serviço — e a segunda é contraintuitiva.** Subprocessos limpos, mediana de 5. **(a) Partida a frio ≈ 1,05 s × predição 1,70 ms (p95 1,75) ⇒ ~620×.** `import pandas+numpy+sklearn` = **0,775 s**; + `joblib.load` = **0,909 s** (o load puxa os submódulos do sklearn, que `import sklearn` não traz); `import fastapi+pydantic+uvicorn` = **0,138 s**. ✅ Confirma o 9d: **carregar o artefato uma vez no startup e manter o processo vivo** — e o custo é **import de biblioteca científica**, não o modelo. A aula afirma que o Lambda instancia *"em questão de milissegundos"*: verdadeiro para o runtime, falso para este processo. **(b) 🚨 O artefato serializado existe por IDENTIDADE, não por velocidade:** `fit` do pipeline campeão inteiro = **13,5 ms** contra `joblib.load` em processo frio = **135 ms** ⇒ **treinar do zero é 10× mais rápido que desserializar**. Pelo critério de desempenho, a API deveria retreinar no startup. Duas razões independentes impedem, **nenhuma delas desempenho**: (i) o dado bruto é Telco/LGPD e o `.dockerignore` do 9f o mantém fora da imagem — retreinar exigiria enfiá-lo num container publicável (e `data.dividir()` custa mais **0,645 s**); (ii) **identidade** — o serviço tem de servir *o mesmo* modelo caracterizado pelo item 85 (`\|PR-AUC − 0,6646\| ≤ 1e-4`), não um retreinado a partir do mesmo código, que deixa de ser o mesmo assim que uma versão de biblioteca muda. 🔑 *Quando o modelo é barato de treinar, o artefato deixa de ser um cache e passa a ser só um contrato de identidade* — e aí precisa de **mais** teste, não menos: cache errado aparece como lentidão, contrato de identidade errado aparece como probabilidade errada. **Reforça 9d-quinquies e o item 66 (round-trip do objeto serializado)** | 9 | ⬜ |
| 93 | M04-A02 (§2c, §3, §4) | **Etapa 11 — a defesa de "por que não microsserviços?", um número de limitação e uma linha de processo.** (a) 🎯 **Sam Newman, citado pela própria aula:** o monólito *"continua sendo escolha válida e muitas vezes o padrão sensato"*, e ele *"precisa ser convencido do porquê usar microsserviços, em vez de procurar razões para não usá-los"* — mais o contraexemplo do **Nubank**, que começou com microsserviços no dia 1 e **ficou mais lento no início** por ser equipe pequena. Grupo pequeno, um modelo, um consumidor, prazo fechado ⇒ **um serviço**. É a defesa vinda de quem é referência *a favor* de microsserviços, e é a mesma disciplina do 9h um andar acima: *recusar a arquitetura é o conteúdo da aula sobre arquitetura, não uma esquiva dele.* (b) ⚠️ **O argumento de separação no TC não é hardware, é ciclo de vida:** a justificativa usual da aula (*"inferência exige CPU intensa, GPU, muita memória"*) **não vale** para uma LogReg de 7,4 KB que responde em 1,7 ms — o que vale é o item 80(a) (*o contrato permanece enquanto o modelo muda*). (c) 🔑 **Limitação com número: artefato 7,4 KB × imagem 253,2 MB = 1 : 34.000.** Se o artefato for assado na imagem, promover um modelo novo republica 34 mil vezes mais bytes do que a mudança real — quantifica o acoplamento que resta e aponta a evolução (registry de modelo / volume). 📌 **Declarar, não construir:** montar registry agora é mudar a forma do que ainda nem funciona. (d) ✅ **Chaos Engineering nomeia o que o repo já faz duas vezes** (itens 79 e 85: injetar `# noqa: C901` para provar que o RUF100 acusa; remover `Gender` para provar que a caracterização reprova) — *"se você quer ter certeza de que aguenta falhas, cause falhas e veja o que quebra"* ⇒ 🔑 *controle que nunca falhou não foi testado, foi presumido.* (e) ⚠️ **Vocabulário:** a aula descreve **Conway's Law** por três páginas (Amazon reorganiza os times para mudar a arquitetura) e **nunca a nomeia**. ⚠️ **Fora do escopo:** Kubernetes, service mesh, Hystrix/Eureka/Zuul, BFF, Fargate, Mangum, canário 90/10, circuit breaker — **o mais tentador é o API Gateway**, que numa API de uma rota é um proxy com nome pomposo | 11 | ⬜ |
| 94 | M04-A03 (§2b, §2c) | 🚨 **A validação da API precisa ser de DOMÍNIO, não só de esquema — e o domínio sai do artefato.** Medido contra o campeão e as 1.409 linhas da validação: a validação "campo presente + `float()`" aprova com **HTTP 200** quatro casos que corrompem a predição — `Contract = "Vitalicio"` (categoria inexistente), `Tenure Months = -999`, `Monthly Charges = -999`, `Total Charges = 1e9`. **10 de 10 categóricas aceitam lixo em silêncio** (`handle_unknown="ignore"` ⇒ valor novo vira linha de zeros = *categoria de referência*, não *não sei*). As numéricas são piores: **`Tenure = -999` ⇒ P(churn) = 1,0000 nas 1.409 linhas**, com **878 clientes (62,3%)** cruzando o limiar de 0,29; `Contract` inexistente move **153 (10,9%)**. ⚠️ `-999` é a sentinela de nulo mais comum em sistema legado — exatamente quem consome uma API interna. ✅ **Correção medida e barata:** gerar o modelo Pydantic **do artefato** — `pipe.feature_names_in_` (13 nomes, mesma ordem que `config.FEATURES`, verificado) e `ohe.categories_` (**25 valores permitidos em 10 colunas**) ⇒ `Literal[...]` + `Field(ge, le)` + `alias` + `extra="forbid"`. **Fecha 4 de 4, ~15 linhas, zero constante à mão.** 🔑 *A fonte de verdade do contrato é o artefato, não o `config.py`* — o config acompanha o **código**, o artefato acompanha o **modelo servido**; isto **substitui** a recomendação do item 89 de derivar os aliases de `config.FEATURES`. ⚠️ Faixas numéricas são **decisão, não medição**: o treino dá `Tenure 0–72` / `Monthly 18,40–118,75` / `Total 18,85–8.684,80`, mas colar o `le` no máximo do treino rejeitaria cliente legítimo de 73 meses — usar limites de negócio folgados (0–120, 0–200, 0–12.000) e declarar a escolha → ✅ **FECHADO em 17/08/2026 (9d):** `src/api/schema.py` gera o modelo Pydantic do artefato — `Literal` das 25 categorias, `Field(ge,le)` das 3 numéricas, `alias` dos nomes reais, `extra="forbid"`. Os quatro payloads dão **422**, com teste parametrizado. As faixas ficaram como decisão declarada (0–120 · 0–200 · 0–12.000) **com teste que exige faixa para toda numérica do artefato** — sem ele, uma coluna nova entraria sem limite e em silêncio | 9 | ✅ |
| 95 | M04-A03 (§2a, §2d, §2f) | 🚨 **CORRIGE o item 88: `extra="forbid"` NÃO fecha o vazamento de `CustomerID` — ele o concentra.** Medido no Pydantic 2.13: o erro `extra_forbidden` devolve `loc: ["CustomerID"]` **e** `input: "3668-QPYBK"`. Antes o dado pessoal vinha diluído no eco do objeto inteiro; agora vem sozinho, rotulado e no topo da lista de erros — por byte de resposta é **pior**. O que `forbid` faz de bom é outra coisa (impedir que o campo entre em silêncio, item 94). ✅ **Antídoto real: handler de `RequestValidationError` que remove `input` e `ctx`** antes de responder **e antes de logar** (`loc` pode ficar — nome de campo não é dado pessoal). **O mesmo handler resolve o 500:** medido, `str(e)` do sklearn cita o dado do cliente (`could not convert string to float: 'setenta reais'`) — e o material manda `'mensagem': str(e)` quatro páginas depois de escrever *"em produção real é melhor não expor detalhes"*. 🔑 *O 500 diz **que** falhou; o log diz **por quê**.* 📌 Junto: **não ecoar a entrada no 200** (o material devolve `entrada_recebida: dados` como campo de contrato) — a resposta leva probabilidade, limiar, decisão e identidade do modelo; correlação por `request_id` gerado no servidor. 📌 E **`debug` nunca de literal**: `app.run(debug=True)` devolve **15 KB** com traceback, **variáveis locais de cada frame** e o **console interativo do Werkzeug** (`CONSOLE_MODE` verificado no corpo) — env var com default desligado. Sem isso, `logs/*.jsonl` (LGPD no `.gitignore`, item 70) recebe o identificador **sem ninguém ter escrito a linha que o coloca lá** → ✅ **FECHADO em 17/08/2026 (9d):** handler de `RequestValidationError` sem `input`/`ctx`, handler de `Exception` que não devolve `str(e)`, resposta que **não ecoa a entrada** e `request_id` gerado no servidor (corpo + header `X-Request-ID`). ⬜ **`debug` por env var não se aplica** — é Flask; o FastAPI não expõe console interativo, e não há `debug=True` no repo | 9, 10 | ✅ |
| 96 | M04-A03 (§2g, §2h, §3) | 🚨 **Identidade do artefato: versão divergente não impede a carga, e o carimbo é apagado no load.** Forjando `_sklearn_version = "1.5.2"` e carregando com sklearn 1.9.0: **21 `InconsistentVersionWarning`** (subclasse de **`UserWarning`** ⇒ não interrompe nada), **o modelo prediz mesmo assim** (0,6601), e **`BaseEstimator.__setstate__` faz `state.pop("_sklearn_version")`** ⇒ o objeto carregado **não sabe mais com que versão foi treinado**. O `requirements.in` tem a razão do pin escrita em comentário (*"pode falhar ou, pior, se comportar diferente"*) e nenhum mecanismo que a aplique. **Duas correções:** (a) **uma linha** em `[tool.pytest.ini_options].filterwarnings` — `"error::sklearn.exceptions.InconsistentVersionWarning"` ao lado do `ConvergenceWarning`, que já está lá pelo argumento idêntico e escrito (*"um warning que ninguém lê é um warning que não existe"*); (b) gravar a versão **ao lado** do artefato ao promover, senão o `/health` de prontidão não tem de onde declará-la. Complementa o item 85: a caracterização prova que o modelo **se comporta** igual, o carimbo prova que **é** o mesmo. 📏 **Dimensionamento do container:** RSS de um worker = **193,6 MB** (`python` 13,1 → `+numpy+pandas` 92,7 → `+sklearn+scipy` 162,8 → `+fastapi+pydantic+uvicorn` 177,8 → `+artefato` 193,6; estável após 200 predições). **93% é import**; o artefato de **7,4 KB em disco custa 15,8 MB em RAM**. 4 workers ≈ **774 MB** ⇒ container de 512 MB comporta **2** — 🔑 *o nº de workers sai da RAM, não da vazão* (a 1,7 ms por predição, a vazão sobra desde o primeiro). 🎯 **Etapa 11:** escrever *"escolhemos FastAPI pelo **contrato** (Pydantic + OpenAPI), não pela concorrência"* — a justificativa por assincronismo do material não se sustenta (Flask tem `async def` desde a 2.0, e `predict_proba` é CPU-bound de 1,7 ms) → ✅ **(a) e (b) FEITOS em 17/08/2026 na Etapa 9c:** a linha do `InconsistentVersionWarning` está no `filterwarnings`, e o carimbo (`python`/`sklearn`/`numpy`/`pandas`/`joblib`) é gravado **dentro** do artefato e **verificado na carga** — divergência levanta `ArtefatoIncompativel` em vez de virar aviso, com teste que forja a versão para provar que barra. ⬜ **Abertos: o dimensionamento (193,6 MB/worker ⇒ nº de workers pela RAM) na 9f e o parágrafo de FastAPI-por-contrato na Etapa 11** | 9, 9.5, 11 | 🟡 |
| 97 | M04-A04 (§2a, §2c) | 🚨 **Dois desenhos da API decididos por medição, e os dois contrariam a intuição.** **(a) `/predict` e `/predict-batch` são `def`, NÃO `async def`.** Mesmo endpoint, mesma carga, 8 requisições concorrentes, trocando só a palavra-chave: a **vazão é idêntica** (`def` 203,1 ms × `async def` 206,4 ms — o GIL serializa de qualquer jeito), mas o **atraso do event loop** (tempo que uma requisição fica presa na fila antes de ser processada) vai de **32,58 ms de máximo** para **206,28 ms** = o lote inteiro. Com `async def` o loop congela do começo ao fim do trabalho síncrono; com `def` o Starlette manda para o threadpool e o pior caso cai para **uma** predição — **6,3×**. 🚨 **Por que morde:** o que fica preso na fila junto é o **`GET /health`**, que é a *liveness probe* do orquestrador (9d-ter) ⇒ healthcheck expira ⇒ **container reiniciado no meio da campanha**, e o congelamento escala linear com o lote. A aula diz *"CPU-bound: o ganho vem de workers"* — verdadeiro e incompleto: a pergunta que decide não é *async ajuda?*, é *async prejudica?*. ✅ Custo zero: `def` é o default. **(b) 🚨 `/predict-batch` é o endpoint PRINCIPAL, não um extra** — medido **825×**: as 1.409 linhas da validação custam **2,853 ms** em uma chamada contra **2.353 ms** em 1.409 chamadas unitárias. O custo do sklearn é quase todo **fixo por chamada** (~1,67 ms); a linha marginal custa **2,0 µs** (1 linha 1,670 ms · 10 → 168,8 µs/linha · 100 → 17,7 · 1.409 → 2,0). 🔑 *A API de churn do TC é intrinsecamente batch* — o caso de uso real é a campanha de retenção rodando sobre a carteira, não um cliente por vez; uma API só unitária força o consumidor a pagar 825× pelo mesmo trabalho. `/predict` passa a ser o caso particular de lote 1. ⚠️ **Com `max_length` declarado na lista**: sem teto, o lote é o vetor de negação de serviço do (a) → ✅ **FECHADO em 17/08/2026 (9d):** todas as rotas são `def`; `/predict-batch` é o endpoint principal com `max_length=5.000` (≈12 ms, cobre a carteira de 7.043 em duas chamadas) e `/predict` é o caso de lote 1, com teste de que os dois concordam. 🚨 **Achado colateral:** eles concordam mas **não bit a bit** — 495 de 1.409 linhas (35%) diferem em até 2,2e-16 por causa do BLAS, com **zero** decisões mudando no limiar | 9, 11 | ✅ |
| 98 | M04-A04 (§2d, §2e, §2f, §2k) | 🚨 **O mecanismo de carga do 9d finalmente tem nome — e NÃO é o `@lru_cache` da aula.** **(a)** Medido em processo frio (mediana de 5): com `@lru_cache`+`Depends`, a **1ª requisição custa 727,9 ms** contra 1,9 ms da 2ª; com **`lifespan`**, o startup paga 713,4 ms **antes de aceitar tráfego** e a 1ª requisição já sai a **1,9 ms** ⇒ **383×**. O custo total é o mesmo (~715 ms, 93% import — A03 §2h): muda **quem paga**, o deploy ou o primeiro cliente. E `uvicorn --workers 4`, que a própria aula recomenda, são **4 requisições de ~728 ms** — a Tabela 3 lista *"cada worker tem sua instância cacheada"* como **vantagem**, que é a descrição exata do custo. 🔑 *`@lru_cache` não é carregamento antecipado; é carregamento preguiçoso com memória.* ⚠️ **`Depends` CONTINUA** — pelo motivo certo, e verificado: `app.dependency_overrides` **vence** o `@lru_cache` (modelo real 0,66009 → dublê 0,99), confirmando o 9d-quater e resolvendo como testar contrato sem pagar 728 ms de carga. **(b) 🚨 O `/health` mente sob DI:** compondo as Figuras 12 e 21 da aula sem alterar uma linha e **apagando o artefato do disco**, `GET /health` → **200 `{"status":"healthy","modelo_carregado":true}`** enquanto `POST /predict` → `FileNotFoundError`. O `except FileNotFoundError` que seria a defesa **não roda mais**: a carga saiu do escopo dele. **Décimo exemplar da família do M03** e o servidor nº 8 da Knight Capital (9d-quinquies) em duas figuras consecutivas ⇒ **o estado de prontidão é lido do objeto que serve, nunca de uma global paralela**. **(c) ✅ `response_model` em TODAS as rotas — o mecanismo de defesa mais barato do módulo:** endpoint devolvendo `CustomerID` + eco da entrada de propósito ⇒ **36 bytes** com `response_model` × **374 bytes** sem; o campo não declarado **não sai**, mesmo que o `return` o inclua. 🔑 *O item 95 pedia disciplina de quem escreve o `return`; o `response_model` transforma isso em propriedade do contrato* — e é a única defesa contra overexposure que não depende de alguém lembrar. 🚨 A aula usa essa ferramenta para o **mal**: declara `entrada_recebida: IrisRequest` **dentro** do `IrisResponse`, e aí o eco do payload vira cláusula publicada no OpenAPI. **(d) 🚨 `httpx` no `requirements.in` é a PRIMEIRA linha da etapa** — item 89b reconfirmado hoje (`RuntimeError: requires the httpx2 package`): era planejamento, virou **bloqueio**, porque os 4 testes de contrato do 9d-sexies e o smoke test são da etapa que começa agora → ✅ **(d) FECHADO em 17/08/2026**, e com uma correção: Starlette 1.6 pede **`httpx2`**, não `httpx` — o segundo ainda funciona, emitindo `DeprecationWarning` a cada import. Declarado no `requirements.in`, lock regenerado (3 dists novas: `httpx2`, `httpcore2`, `truststore`; nenhuma versão existente alterada) e `TestClient` verificado sob `-W error::DeprecationWarning`. ✅ **(a), (b) e (c) FECHADOS em 17/08/2026 (9d)**, com um ajuste que só apareceu implementando: o mecanismo de carga **não é o `lifespan`**, é a factory `criar_app(artefato)` — o contrato é derivado do artefato, então o schema precisa dele **antes de as rotas existirem**, e o `lifespan` roda depois. A factory carrega ainda mais cedo, com o mesmo efeito medido (o deploy paga, não o primeiro cliente). `Depends` ficou, e `dependency_overrides` é o que torna os testes de lógica independentes do disco; `/health` lê do objeto que serve e o processo **morre no boot** sem artefato; `response_model` em todas as rotas | 9 | ✅ |
| 99 | M04-A04 (§2b, §2g, §2h, §2i) | **Quatro fechamentos baratos da Etapa 9/10/11.** (a) ✅ **O item 94 sai confirmado SEM trade-off:** o contrato de domínio derivado do artefato (13 campos, `Literal` de 25 valores, `Field(ge,le)`) custa **0,002 ms** — **0,1%** da requisição, **800× mais barato** que a predição que protege. Decomposição: Pydantic 0,002 · montar DataFrame 0,091 · `predict_proba` 1,683 = **1,815 ms**. Não há o que ponderar, só o que escrever. ⚠️ Nota lateral com consequência se a latência algum dia importar: **montar o DataFrame custa 54× mais que validar** — o alvo seria ele, não a validação. (b) ⚠️ **`examples=[5.1]`, nunca `example=5.1`**: a Tabela 1 e a Figura 7 da aula ensinam a sintaxe do Pydantic **v1**, que no 2.13.4 do repo levanta `PydanticDeprecatedSince20` — e o `pyproject.toml` trata warning como erro (`ConvergenceWarning` hoje, `InconsistentVersionWarning` pelo item 96), então escrever `example=` **quebra a suíte**. Armadilha reproduzível; a própria Figura 7 traz o tooltip `(function) example: Any` tapando uma linha. (c) 🎯 **Etapa 11 — a OpenAPI É a documentação de interface da entrega**, não há segundo documento a escrever; e **declarar que a superfície de `/docs` acompanha a de autenticação**: o `openapi.json` do contrato derivado publica **3.501 bytes com 13 nomes de feature, 25 valores de categoria em 10 colunas e as 3 faixas numéricas**, sem proteção (limitação já declarada no item 90c). Não é dado pessoal — é a **descrição do modelo**. ✅ Manter `/docs` (o benefício em API interna supera, e é o argumento de escolha do framework — item 96), registrando `openapi_url=None` como o que se faz quando a API sair da rede interna. (d) ✅ **Etapa 10 — o middleware de cronometragem custa +13%** (1,909 → 2,164 ms) e **cronometra também o 422**, que é o comportamento certo: sem isso o P95 fica cego justamente para as requisições que falham rápido. É o gancho concreto do 9d-bis-3 (*"um `curl` não é uma medida de latência"*) e o lugar natural do `request_id` do item 95 → ✅ **(a), (b) e (d) FEITOS em 17/08/2026 (9d):** validação de domínio no lugar (custo confirmado irrelevante), `examples=[...]` na sintaxe v2 e middleware que gera `request_id` e cronometra **com `try/finally`**, inclusive o 422. ⬜ **(c) é Etapa 11** — a OpenAPI como documentação de interface e a nota sobre a superfície de `/docs` | 9, 10, 11 | 🟡 |
| 100 | M04-A05 (§2a) | 🚨 **O `default=` do `os.getenv` é um segredo publicado, e o código com a env var faltando FUNCIONA.** A aula escreve `SECRET_KEY = os.getenv("JWT_SECRET", "minha-chave-jwt-super-secreta")` **na página seguinte** ao aviso *"nunca deixe tokens no código!"*. Verificado ponta a ponta: sem `JWT_SECRET` no ambiente (o estado default de qualquer máquina), assinei com a string impressa no PDF um token `{"sub": "atacante", "role": "admin"}` com validade escolhida por mim (**2036**) e as duas rotas protegidas responderam **200** — `/predict` e `/me`, sem nunca ter feito login. A expiração de 30 min que a aula lista como vantagem do JWT **é parâmetro de quem assina**. Bônus do stderr que ninguém lê: a **própria PyJWT** emite `InsecureKeyLengthWarning` (chave de 29 bytes < 32 do RFC 7518) e a Figura 14 usa `datetime.utcnow()`, `DeprecationWarning` no 3.12 ⇒ **não roda neste repo**, que trata warning como erro. E `USERS_DB` compara **senha em texto puro** com `!=` — a ressalva *"use banco de dados"* não corrige isso. ✅ **Regra: segredo ausente ⇒ o processo morre no boot** (`os.environ["X"]`, não `getenv(..., default)`) — que é **exatamente o que o `criar_app()` do repo já faz com o artefato** (9d-octies). 🔑 *Falha de configuração tem de ser barulhenta na inicialização; silenciosa, ela vira a configuração.* | 9, 9f-bis?, 11 | ⬜ |
| 101 | M04-A05 (§2b, §2c, §2d, §2g) | 🚨 **Rate limiting: os três defeitos da Figura 23, e nenhum é de estilo.** **(a) A probe do orquestrador consome a cota e depois toma 429.** O middleware não excetua rota: com o limite da aula (10 req/60 s por IP) e `HEALTHCHECK --interval=10s`, as 6 probes/min comem **60% da cota** e a próxima probe recebe **429** ⇒ o orquestrador lê container doente e **reinicia**. O inverso também foi medido: um cliente satura o IP e a **probe** é a vítima, com a API perfeitamente saudável. 🔑 *É o mesmo desfecho do `async def` da A04 (item 97a) por outra causa — **tratar a probe como tráfego de cliente***. **(b) O limitador some com `--workers`:** uvicorn real, 60 requisições concorrentes do mesmo IP, limite declarado 10 — passaram **10** (1 worker), **31** (4 workers), **37** (8 workers), enquanto o header `X-RateLimit-Limit` continua respondendo **10**. É a contradição interna da aula: a **Tabela 2** exige *"código stateless para escalabilidade horizontal sem inconsistências"* e a **Tabela 3** já indica *"SlowAPI ou API Gateway"* — a Figura 23 é a única com código, e é a que não se usa. 🔑 *Rate limiting é estado compartilhado, logo mora onde o estado é compartilhado (Redis, gateway, ingress).* **(c) O `defaultdict` nunca esquece um IP:** a janela expira os timestamps, a **chave não sai** — **81,2 MB** para 100 mil origens distintas, **804,7 MB** para 1 milhão; um scan de /16 basta, e o container do TC tem 193,6 MB/worker de linha de base (item 96). O componente escrito para conter abuso de CPU vira o caminho para exaurir a RAM. ✅ **O que fica para a 9f, sem implementar limitador nenhum:** `/health` e `/` **fora** de credencial e de qualquer rate limit — verificado que o atalho `FastAPI(dependencies=[...])` protege **tudo**, inclusive o `/health` (401 na probe) ⇒ a dependência vai nas rotas de inferência, e o teste que fixa isso é `GET /health` sem credencial = 200 | 9f, 10 | ⬜ |
| 102 | M04-A05 (§3, §2e, §2f, §2h) | 🎯 **A Tabela 3 é uma rubrica de go-live — usar como estrutura da seção de limitações da Etapa 11, e fechar UMA linha dela.** ✅ **DECIDIDO em 17/08/2026 (Luca): manter a limitação declarada** — a API Key entra **só se** uma aula posterior entregar o conhecimento (mesmo critério da M02-A05). A API é interna, sem frontend e sem usuários; a limitação está **escrita** no `description` do app, e implementar depois custa o mesmo que agora — sem refação de nada. O que fica aberto é **escrever a justificativa na Etapa 11**, não o código. Custo medido na API real (`/v1/predict`, mediana de 400, alternado): **+0,329 ms** por requisição, dos quais **0,095 ms é o mecanismo de DI** e o resto é validar o header — 10% de uma predição unitária e **desprezível no modo batch**, onde é pago uma vez para a carteira inteira. 📌 **Se destravar, o mecanismo é API Key, não JWT** — JWT sem base de usuários é cerimônia (login com dicionário, senha em claro, segredo a gerenciar); API Key é o que a própria aula indica para **comunicação entre serviços**, que é o caso do TC: header `X-API-Key`, chave por **env var obrigatória** (item 100), `secrets.compare_digest`, `/health` e `/` fora, 3 testes. Vira um passo **9f-bis** de ~30 min depois da 9f (o rótulo 9g já é o pacote instalável). **O que é declarar, não implementar:** (i) **HTTPS termina no ingress**, a aplicação fala HTTP na rede interna — decisão, não omissão; (ii) **CORS desabilitado de propósito** (pág. 21: *"server-to-server: desabilitar aumenta a segurança"*) — o TC não tem frontend; ⚠️ se algum dia entrar, **nunca `allow_origins=["*"]` com `allow_credentials=True`**: medido, o Starlette **ecoa a origem do atacante** no `Access-Control-Allow-Origin`, que é permissão universal com credencial — a config insegura é a que "funciona"; (iii) **`/docs` mantido** contra o hardening da pág. 23, pelo argumento do item 99c; (iv) **OWASP API Top 10** citado nominalmente (está na pág. 25, citação de graça) — e a aula ilustra um item dele por omissão: o JWT carrega `role` e **nenhuma rota lê o `role`**. **Etapa 10:** log **JSON** com caminho, método, status e `request_id`, **sem** payload, token ou `X-API-Key` — o item 95 limpou a resposta, falta o log. **9f:** `USER` não-root (menor privilégio, pág. 23) e segredo por `-e`/secret do orquestrador, **nunca `ENV` no Dockerfile** (fica na camada para sempre). 🧊 **Anti-preciosismo verificado:** o `!=` da Figura 4 **não** vaza timing explorável (23,6 / 25,2 / 25,2 / 21,8 ns — sem gradiente; o CPython delega a `memcmp`) — usa-se `compare_digest` porque custa 25 ns, não porque o `!=` seja a falha; a falha daquela figura é o token no código. ⚠️ E a **Figura 20 é de outro código**: com `Header(default=None)` cru o `openapi.json` não tem `securitySchemes` e o botão `Authorize` do print **não existe** — precisa de `fastapi.security.HTTPBearer` | 9f, 9f-bis?, 10, 11 | ⬜ |

## Decisões tomadas sob incerteza — reavaliar se surgir informação

| # | Decisão | Sob qual incerteza | O que a mudaria |
|---|---|---|---|
| 1 | Métrica primária = PR-AUC | assumindo que o consumo é uma fila priorizada | se o negócio exigisse decisão binária automática, voltaria a F2 com limiar fixo |
| 2 | Razão de custo FN:FP = 3:1 | premissas de ARPU, margem e conversão de campanha inventadas por nós | qualquer número real de operação refaz a tabela — e move o limiar de operação |
| 3 | Split aleatório estratificado | imposto pelo dataset ser um retrato sem eixo temporal | se aparecer versão longitudinal do dataset, split temporal passa a ser obrigatório |
| 33 | Execução 9.5 | ~~Push pendente~~ → **FEITO 11/08, CI verde nos 2 jobs.** 🔺 **Mas a previsão se confirmou: o run levou 7min30s** (qa 3min33 + gate 3min55) porque **os dois jobs instalam `torch` separadamente**, e ele só é usado na Etapa 8. Está no limite do que se tolera antes de o CI virar estorvo. Opções: `requirements-ci.txt` sem torch/shap/fairlearn para o job `qa`, ou índice CPU do PyTorch (~200 MB em vez de ~800 MB). Reavaliar depois que o cache do pip aquecer | 9.5 | 🟡 |
| 34 | Execução 9.5 | Quando a Etapa 9 existir: **destravar o 3º job** (registro no Model Registry). Precisa das DUAS peças — `needs:` (ordem) e `upload/download-artifact` (material) — mais `MLFLOW_TRACKING_URI` via **secret** apontando para backend persistente. Os bloqueios estão comentados no fim do `ci.yml` | 9, 9.5 | ⬜ |
| 35 | Execução 9.5 | ~~O piso do gate precisa subir junto com o campeão~~ → **FECHADO na Etapa 8: não subiu, porque o campeão não mudou em nenhuma das três etapas.** O MLP ficou 0,0031 abaixo. O gate ganhou, em vez de piso maior, um **segundo eixo** (Brier ≤ 0,14, item 41). Histórico da Etapa 7: Nenhuma das 58 configurações superou o campeão na validação (0,6646), logo o piso continua em 0,66 — o gate acompanha o campeão, e o campeão não mudou. Foi também o argumento que barrou a adoção da config 1-SE: ela deixaria a margem em 0,0020, menos que a folga de 0,005 reservada à variação entre plataformas. **Continua aberto para a Etapa 8** | 7, 8, 9.5 | 🟡 |
| 36 | M02-A05 (§2) | ~~🚨 **A CV repetida é cega à variância de inicialização**~~ → **MEDIDO e respondido: o protocolo NÃO precisa da dimensão seed.** Desvio entre seeds **0,0008** na rede `(32,)` (0,04× o desvio entre folds de 0,0215) e 0,0035 na configuração sem camada oculta. 🔺 **E o contraintuitivo é o achado:** o modelo **convexo** variou MAIS — porque "variância de seed" agrega três fontes (init, ordem dos lotes, split interno) e sem camada oculta não há mínimo local. O XOR falhava em 14% das inicializações; a conclusão de lá **não transferia**, e só se sabe medindo. Original: — verdade para LogReg (convexa) e para árvores com `random_state`, **falso para o MLP** (superfície não-convexa + init aleatória). A CV repetida 5×3 cobre variância **de partição** e é cega à variância **de inicialização**. Decidir antes de comparar na Etapa 8: basta declarar no decision log que os finalistas são determinísticos e rodar o MLP sobre ≥5 seeds (média ± desvio), ou o protocolo inteiro precisa da dimensão seed? Critério: se o desvio entre seeds for da ordem do desvio entre folds (**0,0188–0,0236**), seed única não sustenta conclusão | 6, 8 | ⬜ |
| 37 | M02-A05 (§1) | 🟡 **Metade feita: o experimento controlado foi executado** (delta LogReg→MLP = −0,0046 no pareado, 5/15 dobras, p=0,064 ⇒ não-linearidade indistinguível de zero). Falta **escrever** na Etapa 11 com a citação. Original: (*"uma rede de uma camada com sigmoide e loss de entropia cruzada é análoga a uma regressão logística"*) para enquadrar a Etapa 8 como **experimento controlado** — LogReg é o MLP de **zero camadas ocultas**, então o delta entre os dois mede *isolada* a não-linearidade do Telco, coisa que a comparação com RF/HGB não permite. Se o MLP não ganhar, é a **3ª medição independente** de *"o sinal é linear no logit"* (as outras: gap 0,005 × 0,083 da Etapa 6; FE inútil da Etapa 4) | 8, 11 | ⬜ |
| 38 | M02-A05 (§2) | *(bônus, se sobrar tempo no vídeo)* O experimento do XOR como ilustração de **mínimo local**: 2 neurônios ocultos falham em **14%** das inicializações; com **5× mais épocas**, os mesmos 14%; com 4 neurônios, **0%**. Mostra em 15 segundos por que "treinar mais" não conserta rede que não aprende — e por que seed é fator, não detalhe | 11 | ⬜ |
| 39 | M02-A06 (§1) · **resolvido pela M02-A07 (§2)** | ~~Formalizar estatisticamente o empate da Etapa 6~~ → **A M02-7 entregou, e entregou algo melhor que um teste: a regra 1-SE** (pág. 12–13, *"escolher o modelo mais simples cujo desempenho esteja a no máximo 1 desvio-padrão do melhor"*, creditada a **Hastie et al., 2009**). Duas consequências: (a) a inspeção da razão feita na Etapa 6 **era o método**, não uma aproximação dele — o que faltava era o nome; (b) a regra **decide**: melhor = HGB 0,6919 ± 0,0188 ⇒ envelope de **0,68705** (SE, K=15) / 0,68349 (K=5 conservador) / 0,67310 (desvio cheio) — **a LogReg 0,6904 entra nas três leituras** ⇒ escolhe-se **a LogReg**, o mais simples dentro do ruído. ⚠️ Declarar que a 1-SE de Hastie foi formulada para um eixo de complexidade **dentro** de uma família (α da poda, λ da Ridge) e que usá-la entre famílias é extensão — sustentável aqui porque "mais simples" tem ordenação inequívoca. ⚠️ **Nadeau–Bengio continua ausente do material** (a aula usa `√K` sem ressalvar que as dobras da CV repetida não são independentes) — por isso o argumento foi construído para não depender de uma única leitura da dispersão. Ver itens **45** (habilitar t pareado/Wilcoxon) e **43** (Netflix como argumento irmão) | 6, 7, 11 | ✅ |
| 40 | M02-A06 (§3a) | 🎯 **Curva de ganho cumulativo** na documentação/vídeo (pág. 23 da aula: *"para ver o retorno se agirmos sobre top X predições"*). É o `recall@k` do repo em forma contínua — eixo x = fração da base contatada, eixo y = fração de churners capturados, com a **diagonal do acaso** e o **teto estrutural** (`k / prevalência`) desenhados junto. Melhor artefato visual da entrega: fala a língua do negócio sem tradução e os dados já estão calculados. Bônus barato no mesmo lote: **histograma das probabilidades preditas por classe verdadeira** (o Brier em forma de figura — explica visualmente por que o limiar ótimo difere por modelo: 0,29 / 0,27 / 0,22) | 11 | ⬜ |
| 41 | M02-A06 (§5) | ~~🚨 **O gate do CI tem um eixo só**~~ → **FEITO na Etapa 8: `Brier ≤ 0,14` entrou ao lado de `PR-AUC ≥ 0,66`**, com o eixo novo **verificado reprovando** (teto a 0,10 → exit 1) e teste unitário sobre a regra isolada (`gate.aprovado`). Entrou antes de barrar alguém, que é quando custa nada. Original: A aula (pág. 20) traz o padrão de SLA com **duas** condições simultâneas. 🔺 **A Etapa 7 forneceu o número que faltava:** entre os três candidatos da validação, PR-AUC e Brier **andam juntos** (0,6646/0,1339 · 0,6620/0,1342 · 0,6589/0,1351), então hoje o segundo eixo não barraria nada — mas é justamente por estar barato que ele deve entrar antes do MLP, cuja calibração ainda é desconhecida. Fazer **junto da Etapa 8** | 9.5, 10 | ⬜ |
| 42 | M02-A06 (§2a, §2b) | **Documentação — duas citações de baixo custo e alto retorno:** (a) **Saito & Rehmsmeier (2015)**, *The Precision-Recall Plot Is More Informative than the ROC Plot…*, está na **bibliografia oficial da aula** (pág. 26) ⇒ a escolha de PR-AUC deixa de ser raciocínio próprio e passa a seguir a literatura indicada pela disciplina; (b) o **caso do Facebook** (pág. 20: *"usava AUC para escolher modelos candidatos e precision/recall para calibrar a decisão final"*) é precedente nomeado da separação **seleção × decisão** que o repo já pratica. Escrever junto a **hierarquia de três métricas**: PR-AUC seleciona · recall@k reporta valor operacional · custo em R$ decide o limiar | 0, 11 | ⬜ |
| 43 | M02-A06 (§4) | ~~(condicional — só depois da Etapa 7)~~ → **A condição se realizou: o tuning NÃO abriu vantagem.** Na validação, campeão 0,6646 · LogReg 1-SE 0,6620 · HGB 1-SE 0,6589 — os três em 0,0057 contra desvio entre folds de 0,022. O desempate é por **interpretabilidade e custo operacional**, e o **Netflix Prize** é a citação (vencedor de US$ 1M nunca implantado: ganho marginal não pagou a complexidade). Escrever na Etapa 11 junto da tabela de odds ratios | 6, 11 | 🟡 |
| 44 | M02-A06 (§6) | **Limitação a declarar:** todas as métricas do TC são **offline**. A aula (pág. 20) distingue offline × online (Google: precisão dos resultados × satisfação medida por A/B) e o TC não tem usuários para medir o lado online — a conversão real da campanha de retenção é inobservável no escopo. Declarar explicitamente na Etapa 11, junto do pressuposto de custo 3:1 | 11 | ⬜ |
| 45 | M02-A07 (§2a) | ~~O repo descarta o dado que o teste pareado exige~~ → **FEITO** (`comparacao.py` guarda `scores`), e o teste pareado rendeu o achado mais instrutivo da Etapa 7: o HGB vence a LogReg em **13 de 15 dobras** com **p<0,001** (t e Wilcoxon) e mesmo assim **perde na validação**. Significância ≠ relevância: o pareamento remove a variância da partição e ganha poder para detectar +0,0113, que em reais são R$ 278/ciclo (0,9%). Usado como evidência corroborante da 1-SE, o papel atribuído a ele **antes** de ser calculado | 6, 7, 11 | ✅ |
| 46 | M02-A07 (§3) | ~~Nested CV — o viés que a Etapa 7 introduz~~ → **PRÉ-REGISTRADO antes de rodar e depois MEDIDO com grupo de controle.** O campeão (0 candidatos) cai 0,0258 da CV para a validação; a LogReg (18 candidatos, com 1-SE) cai o mesmo (excedente +0,0018); o HGB (40 candidatos) cai 0,0384 (excedente **−0,0126**). O viés cresce com a grade, como a aula prevê — e foi suficiente para **inverter o ranking** dos dois finalistas. Reportar o pico do HGB (0,7007) anunciaria +0,0088 sobre o campeão; o número honesto é −0,0057 | 7, 11 | ✅ |
| 47 | M02-A07 (§4) | ~~Estratégia de busca por finalista + 1-SE dentro da grade~~ → **FEITO**: grid na LogReg (18 configs, 15s), random search no HGB (40 configs, 22s), `refit=<callable>` com a regra 1-SE em ambos. A 1-SE trocou o pico nos dois casos, por −0,18 dp e −0,16 dp. Justificativa medida: **12 de 18** candidatos da LogReg caem dentro do envelope, ou seja, o `argmax` estaria escolhendo entre doze empates. `HalvingGridSearchCV` (plano B) não foi necessário — a etapa inteira roda em 37s | 7 | ✅ |
| 48 | M02-A07 (§5a) | **Pressuposto de estacionariedade a declarar.** A aula dedica espaço a *blocked CV*, blocos deslizantes e *gap validation* (pág. 11), com a regra *"o fold de validação deve ser sempre posterior aos dados de treino"*. **Não se aplica** ao TC — o Telco é um snapshot sem data de observação, e `tenure` é uma *variável* (meses de casa), não um índice temporal, então embaralhar não mistura futuro com passado. Mas a validade da CV embaralhada **embute o pressuposto de que a relação features→churn é estacionária** — exatamente o que a Etapa 10 monitora como *concept drift*. Uma linha na Etapa 11: a validação assume o que o monitoramento existe para vigiar. Casa com a decisão sob incerteza nº 3 (split aleatório) | 11 | ⬜ |
